# STELLA FABLE MOONSHOT — Registro de Riesgos

> Última actualización: 2026-07-31 (reconciliación documental, checkpoint `15af6bb`; registro base creado en el bootstrap sobre `dd36a4e`, actualizado durante toda la campaña)
> Severidad: P0 = bloquea el release candidate offline · P1 = degrada calidad/confianza · P2 = mejora.
> Estado: ABIERTO / MITIGADO / ACEPTADO (con firma de decisión en DECISIONS.md).
>
> **Correcciones de esta reconciliación** (a partir de la auditoría independiente
> `STELLA_MOONSHOT_INDEPENDENT_VERIFICATION`): RK-01 marcado con su wiring
> resuelto; RK-04 confirmado como PREPARADO (no CERRADO — la aplicación sigue
> siendo G2); RK-27 corregido de ABIERTO a MITIGADO con evidencia de
> `eval:roles`; RK-13/RK-13b y RK-34/RK-34b fusionados en una única entrada
> vigente cada uno (sin perder el historial de ambos hallazgos). RK-28 y
> RK-29 se mantienen ABIERTOS — no hay evidencia de código que los cierre.

## P0 — Bloqueadores del Offline Release Candidate

**Resumen (checkpoint final `15af6bb`): 8 de 9 P0 MITIGADOS offline; 1 (RK-04) PREPARADO, pendiente de aplicación en G2.**

| ID | Riesgo | Evidencia | Workstream | Estado |
|----|--------|-----------|------------|--------|
| RK-01 | Paridad de contexto rota (7 campos nunca poblados) | merge `24b122c` | WS1 | **MITIGADO** (18/18 campos poblados con queries org-scoped; test de paridad estricto; readiness vivo inyectado en `advisor.ts:139` — wiring del coordinador RESUELTO en `ea892ca`, verificado en código) |
| RK-02 | R1–R6 abiertas en código | merge `24b122c` | WS1 | **MITIGADO OFFLINE** (R1 sentinelas, R3 slices por step, R4 validador de fugas ±corchetes, R5 fixtures completos, R6 refusal categórico; R2 pertinencia sigue siendo heurística — validación semántica plena queda para G1) |
| RK-03 | Advisor contextual sin UI | merge `0d0791a` | WS2 | **MITIGADO** (StellaContextualAdvisorPanel montado en los 7 steps con fuentes legibles y ciclo completo; convivencia con panel legacy pendiente de DP-03) |
| RK-04 | `stella_interactions` sin trigger append-only + grants UPDATE/DELETE | `db/prepared/stella_0002_*.sql`, `tests/integration/rls.test.ts` (casos staged) | WS3 | **PREPARADO** (SQL con rollback + tests RLS duales pre/post; la APLICACIÓN es gate externo G2 — ver G2_PACKAGE.md) |
| RK-04b | **Garantía append-only evadible vía `TRUNCATE`** en las 4 tablas protegidas. Un trigger `FOR EACH ROW` no se dispara en `TRUNCATE` y RLS tampoco lo gobierna. **Demostrado** sobre PostgreSQL 17 real (2026-08-01): `SET LOCAL ROLE authenticated; TRUNCATE TABLE public.stella_interactions;` **tuvo éxito** (1 fila → 0; revertido con ROLLBACK). **Causa raíz:** los `ALTER DEFAULT PRIVILEGES` de Supabase conceden `Dxtm` (TRUNCATE/REFERENCES/TRIGGER/MAINTAIN) a `authenticated` sobre toda tabla creada en `public` — **ninguna migración de este repo lo concedió**; `0033` solo añade `ar` encima. **Alcance sistémico: 36 de 37 tablas** lo heredan (la excepción, `signup_allowlist`, solo porque `0033:69` hace `REVOKE ALL`). Severidad **MAJOR**: sin ruta de explotación alcanzable hoy (`authenticated` es NOLOGIN, PostgREST no expone TRUNCATE, 0 funciones con SQL dinámico, sin `CREATE` en ningún schema), pero rompe una garantía documentada | `db/prepared/stella_0002b_*.sql`; auditoría `STELLA_APPEND_ONLY_PRIVILEGE_AUDIT` | WS3b | **PREPARADO** (0002b: revoca los privilegios sobrantes **y** añade 4 triggers `BEFORE TRUNCATE FOR EACH STATEMENT` — única capa que alcanza al **owner**, que aquí importa porque `db/client.ts` conecta como `postgres`. Ensayado localmente; la aplicación real es G2). **No invalida RK-04/stella_0002**, que hizo exactamente lo que declaraba |
| RK-04c | **Los `ALTER DEFAULT PRIVILEGES` globales siguen concediendo `Dxtm`**, así que toda tabla nueva creada en `public` renace con el defecto de RK-04b. `stella_0002b` repara las 4 tablas existentes pero **no** toca la causa raíz; `stella_0003` se endureció con `REVOKE ALL` antes de su primera aplicación para no heredarlo | `pg_default_acl`; `db/prepared/stella_0003_suggestion_decisions.sql` §4 | WS3b | **CERRADO EN LOCAL 2026-08-02** por `db/prepared/stella_0004_role_separation.sql` — éste era el gate transversal. Corrige `pg_default_acl` de **`postgres` y `supabase_admin`** en `public`, revoca `Dxtm` a `authenticated`/`service_role` en las **38** tablas (no sólo las 4 de `0002b`) y traslada el ownership a `uellix_owner`, cuyos objetos futuros nacen sin ningún grant de terceros. **Dos hallazgos nuevos** salieron del ensayo: (a) el `pg_default_acl` de `supabase_admin` concedía los **8** privilegios a `anon` — peor que el `Dxtm` original y también invisible en las migraciones; (b) `ALTER DEFAULT PRIVILEGES … IN SCHEMA public REVOKE … FROM PUBLIC` **no funciona** sobre funciones y tipos (0 filas, reporta éxito): sólo la forma **global** suprime el `EXECUTE`/`USAGE` incorporado. **Sigue ABIERTO en remoto** — ver RR-03 y RR-09 en `docs/ops/DATABASE_ROLE_MODEL.md` |
| RK-04d | **Guarda de escritura de `stella_0003` vacua bajo superusuario** (MAJ-A de la reauditoría). Usaba `has_table_privilege(current_user, …)`, que devuelve `true` para cualquier rol con `rolsuper` — ciega precisamente en el escenario que su propio comentario nombraba (aplicar vía tooling como `supabase_admin`, que **sí** es superusuario) — y además probaba el rol *instalador*, no el de la aplicación. Acompañado de **MAJ-B** (sin auto-verificación final: un `REVOKE` sólo quita concesiones del grantor actual y **avisa**, no falla) y **MAJ-C** (sin guarda de existencia de roles) | `db/prepared/stella_0003_suggestion_decisions.sql` §0-pre, §4b, §7 | WS3b | **CERRADO antes de la primera aplicación.** Guarda reescrita sobre hechos auditables (rol escritor **declarado** vía `stella.writer_role`, owner real, ACL **directa** por `aclexplode` sin herencia ni atajo de superusuario; exige *owner* **o** *INSERT+SELECT directos + `rolbypassrls`*, porque RLS está activo sin policy INSERT y `RETURNING id` requiere SELECT). Probado empíricamente: **ejecutada por `supabase_admin` (superusuario) con un writer incorrecto, sigue abortando**. Añadidas auto-verificación de 18 puntos en la misma transacción y guarda de roles (dos comprobaciones más se retiraron en la ronda 2 por romper la convergencia y por ser infalsificable — ver db/prepared/README.md). El script **sigue sin aplicarse** |
| RK-04e | **Falso rojo en la verificación 6 de G2** (MIN-B): usaba `information_schema.role_table_grants`, que expande privilegios alcanzados por **membresía** — y `postgres` **es miembro** de `authenticated` y `service_role` (verificado). Sobre `stella_interactions` devuelve **11 filas** frente a **4** concesiones directas reales, así que el gate se habría leído como fallo con el estado correcto | `docs/ops/gates/G2_PACKAGE.md` §6 | WS3b | **CERRADO.** Sustituida por `aclexplode(COALESCE(relacl, acldefault('r', relowner)))`, que lee la ACL literalmente, más un chequeo explícito de `PUBLIC` (`grantee = 0`). Prueba offline impide que vuelva la consulta ambigua |
| RK-04f | **La política de retención describía una cascada de borrado inexistente.** Afirmaba que `applied_text`/`rejection_reason` quedaban "cubiertos por el borrado del proyecto/org (cascada organizacional ya existente)". Verificado: **0 de 27** FKs hacia `organizations` usan `CASCADE`; todas son `NO ACTION` | `docs/ops/STELLA_RETENTION_POLICY.md` §5 | WS3b | **CORREGIDO** (documental). Las filas **bloquean** el borrado del padre en vez de desaparecer con él; se documenta qué hacer antes de borrar, qué conservar por trazabilidad y que **nada está automatizado**. Además, desde `stella_0003` la vía 4.2 (pseudonimizar por `UPDATE`) queda bloqueada por el trigger append-only incluso para el owner. **No se cambian las FKs** en esta unidad |
| RK-04g | **Las aserciones sobre triggers append-only eran infalsificables.** Los dos casos post-G2 usaban `expect(db.execute(...)).rejects.toThrow(/append-only/)`. `db.execute()` de drizzle-orm 0.45.2 lanza un `DrizzleQueryError` cuyo `.message` es `"Failed query: <sql>\nparams: "`; el `PostgresError` real (postgres-js 3.4.9) con `code='42501'` y el mensaje del trigger queda en `.cause`. `toThrow(/regex/)` compara sólo contra `.message`, así que la aserción **nunca podía ver** el mensaje del trigger: **rojo garantizado con la base correcta**. Detectado en la 1ª ejecución de G3 local (23/25); la base sí bloqueó y la fila quedó intacta. Defecto **independiente del entorno** — habría fallado igual en staging | `tests/integration/rls.test.ts`, `tests/helpers/append-only-error.ts` | WS3b | **CERRADO** (2026-08-01). Helper que recorre la cadena `cause` con límite de profundidad y detección de ciclos y exige **conjuntamente** SQLSTATE `42501`, texto `append-only`, operación y tabla — más fuerte que la aserción original, y rechaza un `42501` que no venga del trigger. Fijado por 13 pruebas unitarias. Reejecución completa **32/32** |
| RK-04h | **G3 contamina de forma irreversible la base contra la que corre.** El bloque de `stella_suggestion_decisions` inserta una fila en una tabla append-only: los triggers rechazan `UPDATE`/`DELETE`/`TRUNCATE` para **todos** los roles, incluido el owner, y sus FK `ON DELETE NO ACTION` fijan además la organización, el proyecto y el usuario referenciados. No existe limpieza fila por fila; la única es reset/rebuild del stack. `G3_PACKAGE.md` afirmaba lo contrario ("G3 no escribe estado permanente fuera de las filas de prueba que la propia suite limpia") | `docs/ops/gates/G3_PACKAGE.md`, `tests/integration/rls.test.ts` | WS3b | **MITIGADO EN LOCAL / ABIERTO EN REMOTO.** Local: suite hecha idempotente sobre la clave determinista `g3-local-rehearsal.synthetic.…[0]` — **REUSED** si existe una (deriva org/proyecto/interacción, cero inserciones), **CREATED** si no, **aborta** si hay más de una; verificado 1→1 decisiones y 2→2 interacciones en la 2ª corrida. Documentación corregida. **Remoto: NO AUTORIZADO** — exige una estrategia no contaminante propia (base efímera dedicada o gate de solo lectura sobre filas ya existentes), todavía sin diseñar |
| RK-04i | **La protección del rollback destructivo de `stella_0003` dependía de banderas de `psql`, no de su estructura.** La guarda de autorización era un bloque `DO $$ … $$;` y el `DROP TABLE IF EXISTS` una sentencia **top-level posterior e independiente**. Sin `-v ON_ERROR_STOP=1`, `psql` reporta el error de la guarda y **envía la siguiente sentencia** — el `DROP`; sin `-1`, no hay transacción que revierta. Esas banderas son una convención de invocación, no una propiedad del archivo, y `G2_PACKAGE.md` admite **tres vías de aplicación de las cuales sólo `psql` las acepta** (las otras dos son `supabase db execute --file` y el SQL Editor). **Demostrado empíricamente** sobre PostgreSQL 17.6 en un contenedor desechable: con `psql` desnudo la forma anterior **destruyó la tabla y su fila pese a que la guarda lanzó la excepción**. Severidad **MAJOR** sobre un audit trail append-only que `DROP TABLE` borra junto con sus propios triggers | `db/prepared/stella_0003_rollback.sql`; `tests/prepared-stella-sql.test.ts` (*review round 4*); `docs/ops/LOCAL_STAGING_G2_REHEARSAL.md` | WS3b | **CERRADO antes de cualquier ejecución del rollback** (2026-08-01). Guarda y `DROP` en **un único bloque `DO`**: un `RAISE EXCEPTION` termina el bloque y ninguna sentencia posterior *de ese bloque* corre — semántica del **servidor** dentro de una sola sentencia, no del **cliente** entre dos. El `DROP` se emite como `EXECUTE '<literal fijo>'`, sin concatenación, `format()`, `quote_ident()` ni variables. Autorización **exacta** (`= 'true'`, con `missing_ok`): rechazadas y verificadas `yes`/`y`/`1`/`TRUE`/`True`/`trUe`/`on`/`t`/`' true '`. `-1 -v ON_ERROR_STOP=1` siguen recomendadas como defensa en profundidad, ya **no** como única barrera. Verificado en **24 escenarios** con `psql` desnudo, incluido el par regresión/cierre (forma vieja destruye ↔ forma nueva sobrevive), y **58 mutantes del script, los 58 detectados**. Revisión independiente en **9 rondas, cerrada con 0 BLOCKER y 0 MAJOR**; todos los hallazgos previos cerrados en la misma unidad: ronda 1 (**RK-04j**), 2 (**RK-04k**), 3 (**RK-04l**, 1 BLOCKER), 4 (**RK-04m**), 5 (**RK-04n**), 6 (**RK-04o**), 7 (**RK-04p**) y 8 (**RK-04q**). En la ronda 9 el revisor no logró construir ninguna mutación que cambiara el comportamiento dejando la suite verde. **El rollback sigue SIN ejecutarse contra ninguna base** |
| RK-04j | **Tres huecos del rollback endurecido, detectados por revisión independiente antes de cualquier ejecución.** (a) **El conteo era indigno de confianza bajo `FORCE ROW LEVEL SECURITY`:** `count(*)` está sujeto a RLS y `FORCE` quita el bypass del owner, así que un rol propietario **sin `rolbypassrls`** contaría **0** sobre una tabla poblada — el script habría anunciado *"table is empty — no audit data lost"* mientras destruía el audit trail. **Reproducido** en PostgreSQL 17.6 (owner `NOBYPASSRLS`, policy `USING(false)`): `FORCE` off → 1, `FORCE` on → **0**, fila presente. No se manifiesta como `postgres` sobre imagen Supabase porque ese rol tiene `rolbypassrls = true`. (b) **La autorización podía ser propiedad del ENTORNO, no de la corrida:** un `ALTER DATABASE … SET` / `ALTER ROLE … SET` persistido pre-autoriza toda sesión futura — el mismo defecto de RK-04i, reubicado de las banderas de `psql` a la capa de GUC. (c) **Las pruebas no prohibían un `EXCEPTION WHEN`:** un handler traga el `RAISE EXCEPTION` de la guarda y deja pasar el `DROP`, y **todas** las demás aserciones de la ronda seguían en verde — una edición además *tentadora*, porque un fallo por objeto dependiente sale sin el prefijo `stella_0003_rollback` | `db/prepared/stella_0003_rollback.sql`; `tests/prepared-stella-sql.test.ts`; `docs/ops/LOCAL_STAGING_G2_REHEARSAL.md` | WS3b | **CERRADOS los tres, antes de cualquier ejecución del rollback** (2026-08-01). (a) Guarda de `relforcerowsecurity` **antes** del conteo, simétrica con §4b/§7 del forward, que ya tratan `FORCE`-on como estado abortable — verificada en el escenario S10. (b) La corrección propuesta por el revisor (`pg_settings.source`) resultó **inaplicable**: los GUC placeholder personalizados **no aparecen en `pg_settings`** (medido: 0 filas), así que el chequeo habría abortado siempre o nunca; implementada sobre **`pg_db_role_setting`**, que sí registra ambas formas persistidas, con `split_part` en vez de `LIKE` (trampa MIN-A) — verificada en S11. Límite honesto declarado en el archivo: SQL puede exigir que la autorización sea **de sesión**, no puede distinguir a un operador de un script envolvente; ese tramo es **precondición humana del gate**, con el mismo reparto en tres que §4b del forward. (c) Aserciones que prohíben `EXCEPTION WHEN` y fijan exactamente **un `BEGIN` y un `RETURN`** en el bloque. Cerrados también los MINOR: irreversibilidad reformulada (el DDL de PostgreSQL es transaccional — bajo `-1`, `ROLLBACK` deshace hasta el `COMMIT`), `SET client_min_messages = notice` (S12), `LOCK TABLE … ACCESS EXCLUSIVE` antes del conteo (TOCTOU con `INSERT` concurrente), banner de destrucción sólo en el camino con filas (S13), y autorización fijada **byte a byte** en vez de por enumeración de rechazos, que no habría detectado `IN ('true','on')`, `ILIKE` ni `ANY(...)` |
| RK-04k | **Las guardas del rollback endurecido estaban declaradas pero no probadas como ARMADAS, y dos defectos sólo detectables contra un motor real.** (a) **Mutation testing del revisor:** degradar el `RAISE EXCEPTION` de una guarda a `RAISE NOTICE`, invertir la condición de `FORCE`, o **neutralizar por completo la guarda de autorización** (`IF NOT authorised AND false THEN`) dejaban la suite **entera en verde** — la última porque la aserción de orden `indexOf('RAISE EXCEPTION') < indexOf("EXECUTE 'DROP TABLE")` se satisfacía con la excepción de *otra* guarda. Invariante declarado y no impuesto, un nivel por encima de RK-04j(c). (b) **`pg_db_role_setting` no es legible en todo entorno:** la consulta de provenance habría muerto con un `permission denied` desnudo, sin prefijo, en el camino de emergencia. (c) **Dos bugs que ninguna prueba offline podía ver**, hallados por el dry-run: `operator is not unique: text \|\| "char"` sobre `pg_class.relkind` —rompía **todas** las corridas que llegaban al `DROP`, con 222 tests en verde— y una consulta de objetos dependientes que **no veía las vistas**, porque una vista depende de la tabla a través de su regla de reescritura (`classid = pg_rewrite`), no de `pg_class`: la guarda existía, no hacía nada y reportaba éxito | `db/prepared/stella_0003_rollback.sql`; `tests/prepared-stella-sql.test.ts`; `docs/ops/LOCAL_STAGING_G2_REHEARSAL.md` | WS3b | **CERRADOS, antes de cualquier ejecución del rollback** (2026-08-01). (a) Cada guarda fijada como **span único condición→`RAISE EXCEPTION`**, más conteo total y prefijo `stella_0003_rollback aborted:` obligatorio; revalidado con **17 mutantes propios, los 17 detectados**, restaurando el archivo byte a byte tras cada uno. (b) Chequeo explícito de `has_table_privilege` antes de leer el catálogo: una guarda no verificable se trata como fallida, con mensaje prefijado. (c) `relkind::text` explícito, y consulta de dependientes reescrita sobre `pg_describe_object()`, que resuelve cualquier `classid` — verificado que detecta vista **y** FK entrante, y nada cuando no hay dependientes. **Guarda añadida:** precondición de **propiedad** (`pg_has_role` contra `relowner`) antes del `LOCK`, porque al adelantar el `LOCK` un rol no dueño pasaba a fallar con `permission denied` sin prefijo; leer `pg_class` no exige privilegio sobre la tabla, así que el rechazo es siempre alcanzable y prefijado. Total: **6 guardas**. Lección de proceso: el lint estático sobre expresiones regulares no resuelve tipos ni recorre catálogos — la Fase de dry-run contra un PostgreSQL real no es opcional |
| RK-04l | **Una guarda añadida por conveniencia habría roto el rollback por completo, y la fixture de ensayo lo ocultaba.** El pre-chequeo de objetos dependientes (adoptado como NIT en la ronda 1 de revisión, **fuera del encargo**) abortaba en **toda** ejecución contra la tabla real: `CreatePolicy()` y las expresiones `CHECK` registran filas `DEPENDENCY_NORMAL` **por columna referenciada**, sin degradación de la auto-referencia, así que un filtro `deptype='n'` las clasifica como dependientes ajenos. **Reproducido** en PostgreSQL 17.6 contra el conjunto de objetos real de `stella_0003`: reportaba la propia política y el propio `CHECK` de la tabla y habría abortado incluso con la tabla vacía. **La fixture de dry-run era la causa de que no se viera**: tenía sólo la tabla y los dos triggers, sin política ni `CHECK`, así que el grafo de dependencias real nunca se ejercitó. Acompañado de: (b) el filtro de persistencia indexado por `current_user` cuando PostgreSQL aplica `pg_db_role_setting` por **`session_user`** (`process_settings(databaseid, GetSessionUserId())`), lo que **falla abierto** justo cuando el operador reejecuta con `SET ROLE` siguiendo el consejo de la guarda de propiedad; y (c) cuatro mutantes más que la suite no detectaba, porque las aserciones fijaban las **condiciones** de las guardas pero no el **flujo de datos** que produce los valores que evalúan — `IF n_rows > 0` → `IF n_rows > 1000000` bastaba para que una tabla poblada y sin autorizar cayera en la rama "vacía" y fuera destruida | `db/prepared/stella_0003_rollback.sql`; `tests/prepared-stella-sql.test.ts`; `docs/ops/LOCAL_STAGING_G2_REHEARSAL.md` | WS3b | **CERRADOS, antes de cualquier ejecución del rollback** (2026-08-01). (a) Guarda **RETIRADA**, no arreglada por tercera vez: dos defectos en dos intentos, más un hueco conocido —dependientes mediados por el **tipo compuesto** de la tabla, que se registran contra `pg_type`— muestran que re-derivar `findDependentObjects()` en SQL no es tarea de este script. Su valor era sólo el **prefijo del mensaje**; no impedía ninguna destrucción. Un fallo por objeto dependiente sale ahora con el mensaje nativo de PostgreSQL, **sin destruir nada**, documentado en `G2_PACKAGE.md`. La tentación que la justificaba —un `EXCEPTION` handler para recuperar el prefijo— se cierra directamente con el test que lo prohíbe. Quedan **cinco** guardas. **Principio registrado: una guarda sólo a veces correcta es peor que ninguna, porque invita a creerle.** (b) `s.setrole = (SELECT oid FROM pg_roles WHERE rolname = session_user)`, coincidencia exacta de OID. (c) La **región de decisión** fijada como **span adyacente verbatim** más escritura única por variable. Fixture de dry-run ampliada al conjunto de objetos real. Revalidado con **20 mutantes, los 20 detectados**. Lección de método: fijar *presencia* (un nombre aparece, un mensaje existe, un `indexOf` precede a otro) resultó insuficiente en **tres rondas seguidas**; sólo los spans verbatim condición→efecto cerraron la clase |
| RK-04m | **Dos formas de derrotar el rollback que la suite no veía, ambas fuera del alcance de lo que se estaba fijando.** (a) **La lista de sentencias top-level no estaba acotada.** Ninguna aserción limitaba **cuántas** hay, y una sola línea añadida antes del `DO` derrota una guarda **desde fuera del bloque** — el único lugar al que no llega el argumento estructural de RK-04i: `SET stella.confirm_destroy_decisions = 'true';` deja `authorised` en `true` y `persisted_at` en `NULL` (un `SET` de sesión no está en `pg_db_role_setting`), de modo que **toda ejecución se autoautoriza** y la barrera central del archivo queda vacua — con la suite entera en verde, porque el test que busca esa cadena es una aserción **positiva** ya satisfecha por el comentario de cabecera. Variantes igual de indetectadas: `SET lock_timeout = 0;` (bloqueo `ACCESS EXCLUSIVE` sin cota) y `SET client_min_messages = warning;` (camino destructivo en silencio). (b) **Se fijaba la condición de la rama, no qué rama contiene qué comportamiento:** intercambiar verbatim los cuerpos de `THEN` y `ELSE` anunciaba una tabla poblada como *"table is empty — no audit data lost"*, se saltaba las tres guardas de autorización, el banner y el `WARNING`, y la destruía | `tests/prepared-stella-sql.test.ts`; `db/prepared/stella_0003_rollback.sql` | WS3b | **CERRADOS** (2026-08-01). (a) Se fija la **lista exacta** de cuatro sentencias top-level, más los literales leídos del fuente crudo, más la prohibición de cualquier `SET stella.` **ejecutable** (con cadenas blanqueadas, porque el mensaje de aborto sí lo menciona legítimamente). (b) Se fija **qué arma cada rama**: las cinco guardas, el banner y el `WARNING` entre `IF n_rows > 0 THEN` y `ELSE`; el `NOTICE` de rollback técnico después del `ELSE` y antes del `DROP`; exactamente un `ELSE`. Revalidado con **25 mutantes, los 25 detectados**. También cerrados: el mensaje de la guarda de `FORCE` ofrecía un remedio (`BYPASSRLS`) que no puede levantar un rechazo incondicional; y el chequeo de terminación de literales `EXECUTE`, declarado "por archivo", **no existía para `stella_0002b`** pese a usar la misma construcción — elevado a helper compartido y aplicado a todos los scripts preparados. **Un NIT rechazado con evidencia:** se sostuvo que los `CHECK` registran `deptype='a'` y no `'n'`; remedido en PG 17.6, ambos `CHECK` **y** la política tienen filas `'n'` por columna referenciada además de la `'a'`, y la tabla medida quedó citada verbatim en el script. **Ninguno de los dos MAJOR afectaba al SQL entregado**: los dos eran huecos de las pruebas |
| RK-04n | **Las aserciones de fragmento no pueden cerrar la clase: cinco rondas, cinco escapes.** En cada ronda de revisión se añadieron más aserciones que fijaban *presencia* o *forma local* (un nombre de catálogo aparece, un mensaje existe, un `indexOf` precede a otro, un span condición→efecto), y en cada ronda apareció una mutación nueva que las respetaba todas: condición ampliada (`AND false`), el flujo de datos en vez de la condición (`IF n_rows > 1000000`), cuerpos de `THEN`/`ELSE` **intercambiados**, un `SET` de autoautorización **top-level**, un `IF false THEN` **envolviendo** una guarda ya fijada una línea más afuera, `persisted_at := NULL;` insertado antes de su propia guarda, y `PERFORM set_config('stella.confirm_destroy_decisions','true',true)` **dentro** del bloque `DO` —donde la lista de sentencias top-level no alcanza—, que deja `authorised` en `true` y `persisted_at` en `NULL`, de modo que la guarda de persistencia **confirma como legítima una autorización falsa**. Cada fragmento fija una forma más y deja el complemento abierto | `tests/prepared-stella-sql.test.ts` | WS3b | **CERRADO por cambio de método** (2026-08-01). Se fija el **cuerpo ejecutable entero** del bloque `DO` —comentarios quitados, literales blanqueados, espacios colapsados— byte a byte contra una constante: todas las sentencias, en orden, con su anidamiento. Blanquear los literales mantiene la aserción legible y separa las dos preocupaciones — esta prueba es dueña de la **estructura**, las pruebas por mensaje del **texto**. Cualquier sentencia insertada, eliminada, reordenada o re-anidada falla. **El coste es deliberado:** editar el SQL obliga a actualizar la constante, así que ningún cambio de lógica ejecutable puede entrar sin aparecer en un diff que alguien apruebe — la propiedad que se quiere en un script que borra un audit trail. Revalidado con **35 mutantes acumulados, los 35 detectados**. Ninguno de los MAJOR de las rondas 4 y 5 afectaba al SQL entregado: los cuatro eran huecos de las pruebas |
| RK-04o | **El supuesto de aislamiento estaba DOCUMENTADO, no impuesto — el mismo defecto que el archivo entero existe para eliminar.** Un comentario declaraba que el cierre de la carrera conteo↔`DROP` valía bajo `READ COMMITTED` y que cualquier otro nivel quedaba "fuera de contrato". Bajo `REPEATABLE READ` o `SERIALIZABLE` el snapshot de la transacción se fija en su primera consulta con snapshot —bajo la invocación prescrita, la primera del bloque, **antes** del `LOCK TABLE`— así que una fila confirmada por otra sesión en esa ventana es **invisible** para el conteo: `n_rows = 0`, se toma la rama `ELSE`, se saltan **las tres** guardas de autorización, y un audit trail poblado se destruye bajo un log que **certifica que no se perdió nada** — el mismo desenlace que la guarda de `FORCE RLS` evita, por otra vía. Decisivo: a diferencia de los cuatro canales de persistencia que el archivo lista correctamente como inobservables (`postgresql.conf`, `ALTER SYSTEM`, `PGOPTIONS`, `options=-c`), **éste sí es observable** — `current_setting('transaction_isolation')` no requiere privilegio alguno. Por la regla del propio archivo, una guarda verificable y simplemente ausente no es un límite honesto: es una guarda que falta. Acompañado de (b) una medición de `pg_depend` rotulada *"Complete output (15 rows)"* que se había tomado contra una fixture **reducida** (sin las 4 FK, con un solo índice) y no era completa | `db/prepared/stella_0003_rollback.sql`; `tests/prepared-stella-sql.test.ts`; `docs/ops/LOCAL_STAGING_G2_REHEARSAL.md` | WS3b | **CERRADOS** (2026-08-01). (a) **Sexta guarda, y la primera del bloque:** `current_setting('transaction_isolation') NOT IN ('read committed','read uncommitted')` → aborta con prefijo nombrando el nivel. Va primera porque bajo un snapshot fijo **todo** lo que el bloque lee es potencialmente rancio respecto al `LOCK`, no sólo el conteo. `READ UNCOMMITTED` se acepta porque PostgreSQL lo implementa **como** `READ COMMITTED`. Verificado en dry-run (S20–S24): `REPEATABLE READ` y `SERIALIZABLE` abortan y la fila sobrevive; `READ COMMITTED`/`READ UNCOMMITTED` proceden sin rechazo falso; y con la **tabla ausente** bajo `REPEATABLE READ` la guarda dispara igualmente, confirmando que corre primero. (b) Remedido contra el conjunto completo de objetos = **20 filas**, pegadas verbatim, con el subconteo anterior señalado. Revalidado con **40 mutantes acumulados, los 40 detectados** (cinco nuevos contra la guarda de aislamiento). **Lección de campaña: un supuesto declarado en un comentario no es una guarda — y la prueba de si debería serlo es si SQL puede observarlo** |
| RK-04p | **La guarda de `FORCE RLS` apoyaba su conclusión en una premisa medida sólo para el dueño exacto, mientras la guarda de propiedad admite un conjunto estrictamente mayor.** `pg_has_role(…, 'USAGE')` deja pasar también a un rol que **hereda** los privilegios del dueño —correcto, es el mismo predicado que usa `DROP TABLE`—, pero la conclusión *"con `FORCE` apagado el conteo es fiable"* se había verificado únicamente con el dueño exacto. El bypass de RLS del owner y la comprobación de propiedad de `DROP` son rutas de código distintas y su coincidencia no puede asumirse: si difirieran, un miembro del rol dueño pasaría ambas guardas, contaría **0** por la política org-scoped sin JWT y destruiría la tabla bajo el log de *"no audit data lost"* — **sin `FORCE` encendido en ningún momento**. Acompañado del incidente que lo siguió: al reformular el mensaje de la guarda de propiedad se introdujo un **apóstrofe sin escapar dentro de un literal SQL** — un error de sintaxis real | `db/prepared/stella_0003_rollback.sql`; `docs/ops/LOCAL_STAGING_G2_REHEARSAL.md` | WS3b | **CERRADO midiendo, no endureciendo** (2026-08-01). Medido en PostgreSQL 17.6 (`FORCE` apagado, RLS activa, política `USING(false)`, una fila): dueño exacto `NOBYPASSRLS` → **1**; miembro `INHERIT` del dueño `NOBYPASSRLS` → **1**; `pg_has_role(miembro, dueño, 'USAGE')` → `true`. **El bypass de RLS sí sigue la membresía, con el mismo predicado que la guarda**, así que el conjunto admitido y el que evade RLS **coinciden** y no existe el hueco; un miembro `NOINHERIT` es rechazado antes por la propia guarda de propiedad. **No se añadió una séptima guarda**: se sustituyó una premisa *afirmada* por una *medida*, registrada junto a la de `FORCE`. El error de sintaxis lo detectó la suite offline **en el acto** (ocho aserciones en rojo, porque el desbalance de comillas propaga) y quedó corregido; verificación posterior: **47 literales, todos balanceados, los 3 con apóstrofe correctamente escapados**, y el script vuelve a parsear y ejecutar contra un PostgreSQL vivo. **Lección: no todo hallazgo se cierra añadiendo una guarda — algunos se cierran convirtiendo una suposición en una medición** |
| RK-04q | **`DROP TABLE` admite un llamador que la guarda de propiedad rechaza — y esa estrechez es PORTANTE sin que nada lo registrara.** El comentario afirmaba que `DROP TABLE` pasa por `object_ownercheck → has_privs_of_role()`; es **incompleto**: `RangeVarCallbackForDropRelation` hace una segunda comprobación y admite también al **dueño del esquema**. **Medido** en PostgreSQL 17.6 (esquema de `sch_owner`, tabla de `tbl_owner`, RLS activa, `FORCE` apagado, política `USING(false)`, una fila): `pg_has_role('sch_owner','tbl_owner','USAGE')` → **false**; `SELECT count(*)` como `sch_owner` → **permission denied**; `DROP TABLE` como `sch_owner` → **ejecutado sin error**. Es decir, **el dueño del esquema puede DESTRUIR la tabla sin poder CONTARLA**. La guarda, al ser más estrecha, es cerrada por defecto y no hay riesgo inmediato — pero el comentario invitaba a "corregirla" para igualarla a la regla documentada, y hacerlo dejaría a ese llamador alcanzar el conteo, obtener 0 (o un error de permisos sin prefijo), tomar la rama *"table is empty — no audit data lost"* y destruir un audit trail poblado, **sin `FORCE` encendido**: el MAJOR de la ronda 7 reintroducido por la única edición que los comentarios hacían parecer un arreglo. Acompañado de: (b) la afirmación *"aplicado a TODOS los scripts preparados"* sobre el chequeo de terminación de literales era falsa —se invocaba desde cuatro `describe`, no desde los rollbacks de 0002/0002b, y nada cubría los `grounding_*`—; y (c) la suite fijaba la estructura por completo pero el **registro operativo** sólo en parte: sobrevivía un mutante que dejaba todas las guardas armadas e **invertía lo que el log dice** sobre la destrucción | `db/prepared/stella_0003_rollback.sql`; `tests/prepared-stella-sql.test.ts` | WS3b | **CERRADOS** (2026-08-01). (a) La guarda se documenta como **deliberadamente más estrecha** que la regla de `DROP`, con la medición citada y el motivo —*ampliarla reintroduce el fallo de la ronda 7*—, con el mismo tratamiento de "anotado para que no lo 'arreglen' luego" que ya tenía la retirada de la guarda de dependientes; un mutante que la amplía al dueño del esquema se detecta ahora. (b) **Barrido de directorio** que aplica `expectNoExecutedDdl` + `expectExecutedLiteralsTerminated` a los **seis** scripts `stella_*`, y el comentario declara que los `grounding_*` pertenecen a `lib/grounding/__tests__/prepared-sql.test.ts` y **no** están cubiertos. (c) Los **cuatro mensajes operativos** quedan fijados **byte a byte** sobre el fuente sin comentarios, más el mensaje de propiedad con su remedio `NOINHERIT`. Revalidado con **54 mutantes acumulados, los 54 detectados**. **Lección: una guarda más estrecha de lo necesario puede ser correcta — pero si nadie registra POR QUÉ, la siguiente revisión la ensancha** |
| RK-04r | **El rollback de `stella_0003` estaba verificado estructuralmente pero nunca había ejecutado.** Diecisiete rondas de endurecimiento (RK-04i…RK-04q), 246 pruebas de fuente de verdad y 58 mutantes detectados fijaban lo que el archivo *dice*; **cero** evidencia de lo que hace contra un motor real. Un script que aborta correctamente en 6 caminos no demuestra que el séptimo —el destructivo— haga lo declarado, y el modo de fallo caro es asimétrico: destruir un audit trail append-only es irreversible después del `COMMIT` | `docs/ops/LOCAL_STAGING_G2_REHEARSAL.md` §STELLA 0003 ROLLBACK REHEARSAL — RUN 1; `STELLA_FABLE_TEST_LEDGER.md` 2026-08-02 | WS3b | **MITIGADO LOCALMENTE** (2026-08-02, HEAD `12715d8`, stack desechable `uellix-stella-g2-local-rehearsal`, PostgreSQL 17.6). Respaldo validado (581 736 bytes, SHA-256 `d46280c4…b436aeb`, `pg_restore -l` → 1155 TOC / 87 `TABLE DATA`) y duplicado fuera de `TEMP` con hash idéntico, **sin restaurar**. Hash del script recalculado **dentro del contenedor** antes de ejecutar. 1ª ejecución con autorización **por sesión** (`-c SET` + `-f` en un solo `psql -1`): exit **0**, **1** fila detectada, banner de 13 líneas y `WARNING` presentes, tabla eliminada. Delta pre/post **cerrado**: −1 tabla, −1 policy, −2 triggers append-only, −3 índices, −7 constraints, −1 grant, y **nada más** (funciones 8→8, migraciones 2→2, `stella_interactions` 2 filas, `organizations`/`users`/`projects`/`organization_members` sin cambio). 2ª ejecución **sin** autorización: exit 0, no-op, cero banner, cero `WARNING`, línea base idéntica byte a byte. **Residual ABIERTO:** volumen mínimo (1 decisión) — no mide contención de `lock_timeout='5s'` frente a un `ACCESS EXCLUSIVE` sobre tabla activa, ni el camino donde el dueño de la tabla no es el rol de conexión. **Un ensayo local no es G2:** la ejecución contra la base real sigue siendo gate externo |
| RK-05 | Harness con scores constantes y detectores sólo sobre summary | merge `24b122c` | WS1/WS6 | **MITIGADO** (scores computados de runs reales de detectores sobre todos los campos de texto; adversarialCasesPassed cuenta pases reales; limitación de short-circuit documentada en G1 §7) |
| RK-06 | Anti-regression con placeholders | merge `2ecd766` | WS3/WS4 | **MITIGADO** (scans fs reales incl. dynamic import/re-export/alias y tx/getDb; probados contra violaciones sintéticas) |
| RK-07 | Prompt injection sin defensa en roles legacy | merge `2ecd766` | WS3 | **MITIGADO** (sobre UNTRUSTED_PROJECT_DATA en los 4 builders — atacado por auditor y resistió; step/sectionType allowlisted tras exploit del auditor; corpus 18 payloads × 6 builders) |
| RK-08 | Poblaciones sensibles: 0% de salvaguardas | merge `c28c135` (`lib/stella/security/sensitive-populations.ts`) | WS3 | **MITIGADO** (detector 5 categorías ES/EN endurecimiento-monotónico, aviso estático en capa confiable fuera del sobre, metadata en audit; bordes de stems documentados fail-safe) |
| RK-09 | PII sin redacción en narrativas + retención indefinida | merges `2ecd766`+`3e967d0` | WS3 | **MITIGADO** (redactPii unicode pre-truncado en todos los caminos libres; retención: política DRAFT DP-04 pendiente de decisión de Lorenzo) |

## P1 — Degradan calidad o confianza

| ID | Riesgo | Evidencia | WS | Estado |
|----|--------|-----------|----|--------|
| RK-10 | Sin dedup/tope de referencias ni fallback contextual | merge `24b122c` | WS1 | **MITIGADO** (dedup orden-preservante, tope 8 distintas fail-closed, fallback contextual schema-válido sólo en ContextualIndexTokenLeakError; índices inválidos siguen PARSE_ERROR por contrato congelado del action) |
| RK-11 | Sin persistencia de decisiones ni historial/undo; Composer con DOM imperativo destructivo | merges `0d0791a`+`3e967d0`+`c28c135` | WS2/WS3 | **MITIGADO OFFLINE** (undo LIFO global con staleness-confirm; composer controlado con confirmación; persistencia: acción dormante + adapter cableado, activación = G2+flag; `interactionId` sigue sin poblarse en la llamada del field — `StellaContextualAdvisorField.tsx` invoca `persistStellaDecision(record, { projectId })` sin `interactionId`, aunque el adapter y la acción ya lo soportan de punta a punta — residual menor, no bloquea C5) |
| RK-12 | Invocaciones/denegaciones Stella invisibles en `audit_logs` | merge `3e967d0` | WS3 | **MITIGADO** (STELLA_INVOKED/DENIED/INTEGRITY_REJECTED/DECISION_RECORDED, metadata-only con canaries de fuga, fire-and-forget) |
| RK-13 | Taxonomía de errores colapsada en paneles (RATE_LIMITED sin reset, TIMEOUT genérico); DISABLED desmonta el panel post-click | Hallazgo original: `StellaAdvisorPanel.tsx:47-57` (auditoría baseline). Mitigación: merge `0d0791a` (`StellaContextualAdvisorPanel.tsx`) | WS2 | **MITIGADO** — *(entrada consolidada 2026-07-31: este ID y el duplicado `RK-13b`, registrado por separado durante Ola 2 sin cruzar contra el registro existente, describían el mismo hallazgo. Se fusionan aquí sin pérdida de evidencia.)* 12 códigos de error con mensajes distintos, reset humanizado tras `RATE_LIMITED`, `DISABLED` como prop inicial del servidor (ya no desmonta el panel post-click) |
| RK-14 | Documentos de evidencia write-only; `content_hash` mutable | `lib/grounding/**`, spec §12 | WS5/WS3 | **PARCIAL** (extracción csv/txt + chunking + retrieval implementados offline; ingest hook y PDF/XLSX = decisión G5). **Fuera del alcance de G2** — *(redacción corregida 2026-07-31: la versión anterior decía "PENDIENTES para G2", lo que podía leerse como que el gate G2 los entrega)*: la signed URL de descarga de evidencia y el trigger de inmutabilidad de `evidence_items.content_hash` **no tienen script preparado**, no figuran en `G2_PACKAGE.md` ni en su addendum, y **no forman parte del gate G2 actual**. Requerirían un futuro `stella_0004` que **todavía no existe** — su creación sería una unidad de trabajo con su propio gate |
| RK-15 | Precisión Decimal global sin fijar y sin golden test del ratio; `parseFloat` en el motor | `lib/pipeline/decimal-config.ts` + goldens | WS4 | **MITIGADO** (merge `5ffbf52`: pin explícito importado por sroi-*/fx-*, goldens exactos re-derivados por auditor, parseNum Decimal con paridad caracterizada) |
| RK-16 | Composer no valida referencias ni cifras contra contexto (alucinación) | `lib/stella/schemas/composer-numeric-guard.ts`, `app/actions/stella/composer.ts:162-205` | WS4/WS6 | **MITIGADO** (guard construido, endurecido post-auditoría y cableado fail-closed en `composer.ts` — wiring del coordinador RESUELTO en `ea892ca`, verificado en código: rechaza el borrador y audita solo conteos de violaciones) |
| RK-17 | Roles reviewer con contexto prestado y flags indescubribles | merge `8f39d2a` | WS6 | **MITIGADO** (contextos por rol con linkage de outcomes; contract test prompt⊆contexto; flags en .env.example; tests de acción y builder) |
| RK-18 | `risk_level=high` del validator no bloquea publicación (advisory) | DP-06 en DECISIONS.md | WS4/WS6 | **DECISIÓN DE PRODUCTO** (elevado a Lorenzo como DP-06; dato persistido y auditado, enforcement sería un check en publicación) |
| RK-19 | `stepMismatch` del proveedor se descartaba sin métrica ni log | merge `c28c135` | WS1/WS7 | **MITIGADO** (surfaced en resultado + console.warn + metadata en audit_logs) |
| RK-20 | CHECK de roles desincronizado 0012↔schema | `db/prepared/stella_0002` | WS3 | **PREPARADO** (DO block idempotente de reconciliación con literales entrecomillados; aplicación = G2) |
| RK-21 | Cualquier miembro (incl. viewer) podía invocar Stella | merge `2ecd766` | WS3 | **MITIGADO** (`canUseStella` set-inclusion en las 5 acciones, antes de cuota; denegación auditada) |
| RK-22 | Sin control de gasto | merges `2ecd766`+`de860ca` | WS3/WS7 | **MITIGADO PARCIAL** (maxOutputTokens/temperature/maxPromptChars activos; tokens+costo estimado visibles en admin; PENDIENTE: cuota por tokens en vez de requests y tope duro de gasto por org — G9 calibra primero) |
| RK-23 | Fallos de Gemini invisibles para Sentry | merge `3e967d0` (`lib/stella/observability.ts`) | WS7/WS3b | **MITIGADO** (captureException con tags/fingerprint por rol+código, mensaje truncado 200c con stack reconstruido — sin fragmentos de prompt) |
| RK-24 | Rate limit con fallback en memoria por instancia sin aviso | merge `c28c135` | WS3/WS7 | **MITIGADO PARCIAL** (warn once-per-process al caer al fallback; la limitación per-instance en serverless persiste por diseño — alerta A2 del plan la cubre; fix estructural requiere KV configurado = operación) |
| RK-25 | Billing UI contradecía el fail-closed (`\|\| 10` falso) | merge `de860ca` | WS7 | **MITIGADO** (3 estados veraces alineados con quota.ts; misma fuente/ventana que enforcement; Stripe relabel) |
| RK-26 | Riesgo de staging accidental de `artifacts/` (prompts/respuestas crudas de evaluaciones reales). NO se mitiga vía `.gitignore` ni `.git/info/exclude`: los artifacts deben seguir visibles en `git status` para auditoría local (D-006). Mitigación: deny del harness sobre `git add artifacts*`/`git add .`/`-A`/`--all`/`-f` + regla de rutas explícitas | `.claude/settings.local.json` (local) | WS3 | MITIGADO (harness, verificado) |
| RK-27 | Fixtures realistas (`audit-fixtures/agua-segura`, generador determinista con seed) nunca conectados al harness de eval — las dos inversiones en realismo no se encontraban | `audit-fixtures/agua-segura/generate.mjs`, `tests/eval/stella-roles/fixture-grounded.ts` | WS1/WS6 | **MITIGADO** — *(corregido 2026-07-31: esta fila decía ABIERTO pese a que el propio registro, en la sección de Riesgos de `STATUS.md` y en `TEST_LEDGER.md` (integración WS6), ya documentaba el cierre; la auditoría independiente lo marcó como contradicción a resolver.)* Conectado por WS6/`eval:roles` gate 5 (`roles-fixture-grounded-deterministic`): `extractAguaSeguraFixtures()` alimenta el caso `roles-evidence_reviewer-fixture-grounded`, extracción determinista verificada (`households=120, testimonios=4, confidence=52`), reproducido en esta reconciliación |
| RK-28 | Redacción de secretos por substring exacto (una key URL-encoded o truncada en el error del proveedor no matchea); `stellaState.missingApiKey` computado y nunca consumido | `gemini-client.ts:124-135`, `config.ts:34` | WS3 | ABIERTO |
| RK-29 | Harness real sólo cubre advisor contextual: validator/composer/reviewer sin harness de proveedor real; sin script pnpm para el runner | `tests/eval/stella-contextual-real/` | WS6 | ABIERTO |

## P2 — Mejoras

| ID | Riesgo | WS |
|----|--------|-----|
| RK-30 | Reformulation declarativo (15%): sin prompt propio, sin trigger, sin UI, sin flag | WS6 |
| RK-31 | Confidence score mide higiene del registro, no calidad del contenido; fallos de recálculo silenciosos | WS5 |
| RK-32 | Búsqueda de proxies: 1 de 8 filtros del spec 15 implementado | WS5/WS7 |
| RK-33 | FX oracle no cableado a inversiones (EUR dead-end); comentario invertido en `rateToUsd` | WS4 |
| RK-34 | ~~Accesibilidad: aria-live condicional, saltos de heading, hex hardcodeado~~ **MITIGADO** en `0d0791a` — *(consolidado 2026-07-31 con el duplicado `RK-34b`, registrado por separado durante Ola 2 sin cruzar contra este ID; misma evidencia, sin pérdida de historial)*: live regions persistentes (no condicionales), foco gestionado al resultado, jerarquía de headings corregida, tokens de color en vez de hex hardcodeado | WS2 |
| RK-35 | Doble build de contexto en `buildAdvisorContextualUserMessage` (punto latente de divergencia) | WS1 |
| RK-36 | ~~Test muerto de funder-breakdown~~ **RESUELTO** en `5ffbf52`: renombrado al glob correcto y reescrito para invocar `buildComposerContext` real (la versión previa nunca llamaba a la función) | WS4 |
| RK-37 | Mensajes de bloqueo del motor mezclan inglés/español | WS4 |

## Riesgos de campaña (proceso)

| ID | Riesgo | Mitigación |
|----|--------|-----------|
| RC-01 | Colisión de archivos entre workstreams paralelos | Zonas calientes con dueño único (DEPENDENCY_MAP §Reglas) |
| RC-02 | Agotar presupuesto dejando ramas incoherentes | Checkpoints por unidad atómica; regla 80 % (MOONSHOT §Presupuesto) |
| RC-03 | Operación remota accidental | Protecciones deny verificadas (FASE G) |
| RC-04 | Declarar éxito sin evidencia | RELEASE_CRITERIA C1–C18 con verificación por comando |
| RC-05 | Auto-auditoría del implementador | Auditor independiente obligatorio por fase (FASE F) |

*(Registro completo tras las 6 auditorías base del 2026-07-31. Fortalezas confirmadas que NO son riesgo: aislamiento organizacional ~90 % (orgId siempre de sesión, ownership check en cada builder), manejo de secretos ~85 % (import dinámico, sin singleton, redacción testeada), harness real con guards cuádruples y checkpointing transaccional ~85 %, motor SROI determinístico ~90 %.)*

## STELLA FULL REBUILD — RUN 2 (2026-08-02) — impacto en el registro de riesgos

RUN 2 no abre riesgos nuevos. Cierra la duda de **reproducibilidad** que RUN 1
dejaba abierta por construcción: RUN 1 se ejecutó sobre un stack con estado
heredado, RUN 2 sobre un volumen creado desde cero minutos antes.

### Riesgos re-evaluados

| Riesgo | Estado antes de RUN 2 | Efecto de RUN 2 |
|---|---|---|
| **RK-04b** — hueco de `TRUNCATE` en tablas append-only | mitigado por `stella_0002b`, verificado una vez | **re-verificado sobre base nueva**: 4 triggers `TRUNCATE` (`tgtype=34`), 8 append-only totales tras 0002b y 10 tras 0003, `TRUNCATE` bloqueado incluso como `postgres`, SQLSTATE `42501` |
| **Reproducibilidad del paquete preparado** — ¿los scripts dependían de estado previo del stack de RUN 1? | **abierto** | **cerrado**: 0002/0002b/0003 aplicados dos veces cada uno sobre volumen nuevo, todos idempotentes, contrato estructural idéntico al documentado |
| **Dependencia del resultado de G3 respecto del estado heredado** | sospecha razonable: RUN 1 corrió en modo `REUSED` | **cerrado**: RUN 2 dio 32/32 en modo `CREATED`. Las 32 aserciones se sostienen por el contrato de seguridad, no por fixtures preexistentes |
| **Aislamiento de stacks locales en un host con tres pilas Supabase** | mitigado por `project_id` único | **re-verificado bajo destrucción real**: allowlist por label Docker, no por prefijo de nombre; `uellix-antigravity` y `aforiq` conservaron IDs de contenedor y estados |
| **Pérdida irrecuperable de filas append-only** | mitigado por respaldo duplicado | **sin cambio**: ambos respaldos reverificados por SHA-256 antes y después, **no restaurados**; RUN 2 nunca los necesitó |

### Riesgos locales que siguen abiertos

| Riesgo | Estado |
|---|---|
| `db:seed:proxies`, `db:seed:taxonomies` y `test:integration` escriben en la BD remota por defecto (sin guarda de host) | **cerrado 2026-08-02** — mitigación ahora **técnica y fail-closed**, no procedimental. Los seeds ya no leen `DATABASE_URL`: resuelven la URL fija de `db/safety/local-stack.ts` y pasan por la capacidad `local_seed`, que no acepta destinos remotos y no tiene bypass. `test:integration` aborta en `vitest.setup.integration.ts` antes de cargar cualquier módulo de test. Ver `docs/ops/DATABASE_TARGET_SAFETY.md` |
| `db/client.ts` lee `DATABASE_URL` sin guarda de host | **cerrado 2026-08-02** — `db/client.ts` ya no construye el cliente al importarse y toda conexión pasa por `assertDatabaseOperationAllowed`. El runtime sigue usando `DATABASE_URL` bajo `app_runtime` (que sí admite remoto, por diseño), pero falla cerrado ante URL ausente, inválida o no clasificable, y los entry points locales quedan fijados al puerto `56322` — no sólo a loopback |
| Un stack local en el **puerto equivocado** pasaba las guardas por hostname | **cerrado 2026-08-02** — riesgo real en este host, que ejecuta tres pilas Supabase en paralelo: `127.0.0.1:55322` (uellix-antigravity) satisfacía una allowlist de hostname. Todas las capacidades locales exigen ahora el puerto esperado |
| Docker Desktop no expone `AppData\Local` en su file-sharing | **menor** — impide el bind-mount read-only para inspeccionar respaldos; la vía por `stdin` lo suple y es más restrictiva |

### Riesgos locales abiertos tras el endurecimiento de acceso a BD

| Riesgo | Estado |
|---|---|
| La allow-list de hostnames de contenedor está vacía por defecto | **aceptado** — un despliegue en red de contenedores debe pasar su propia lista; hasta entonces un hostname sin puntos clasifica `unknown` y falla cerrado. Es la dirección segura |
| Las capacidades `controlled_remote_*` existen pero **ningún** entry point las usa | **abierto por diseño** — son la vía prevista para el día en que una herramienta TypeScript deba operar contra staging. Hoy toda operación remota sigue pasando por `db/prepared/` y las checklists de `docs/ops/gates/`. Su primer uso real exigirá revisión propia |
| `pnpm test:integration` y `pnpm test:rls` conservan su nombre histórico | **aceptado** — CI los invoca; ya pasan por la guarda. `db:test:integration:local` es el alias que declara el entorno |
| Mutación superviviente de la reauditoría independiente: el orden del *merge* de `postgresOptions.connection` no tenía prueba propia (alcanzable sólo si `GUARD_OWNED_CONNECTION_KEYS` fallara), esa comparación era case-sensitive, y `sslrootcert` no tenía prueba dedicada de su comportamiento real | **cerrado 2026-08-02** — `mergeGuardedConnectionOptions()` extraída y probada de forma aislada; comparación normalizada a minúsculas; `sslrootcert` fijado por prueba tras verificación empírica contra `postgres@3.4.9`. 8/8 mutaciones detectadas. Ver `docs/ops/DATABASE_TARGET_SAFETY.md` §9 ronda 6 |

### Separación de roles de base de datos (2026-08-02)

Bloque `stella_0004`. Contrato completo, matriz de privilegios y decisiones de
compatibilidad en [`docs/ops/DATABASE_ROLE_MODEL.md`](DATABASE_ROLE_MODEL.md).

| ID | Riesgo | Estado |
|---|---|---|
| RR-01 | **El runtime era el owner de los 46 objetos de `public`**, así que podía `ALTER`, `DROP`, `DROP POLICY`, `DISABLE ROW LEVEL SECURITY` y `DISABLE TRIGGER` sobre las mismas tablas que los 10 triggers append-only debían protegerle. Un modelo de grants nunca protege una tabla de su propio owner | **CERRADO EN LOCAL** — ownership en `uellix_owner`, alcanzable sólo por `uellix_migrator` con `SET ROLE` explícito. Verificado: `pg_has_role('postgres','uellix_owner','USAGE'|'MEMBER') = false`. **Residual:** `postgres` conserva `rolbypassrls`, así que el backend sigue exento de RLS — rotar el runtime a `uellix_app` exige propagar claims JWT por transacción (**DP-07**) |
| RR-02 | En Supabase gestionado, un rol `CREATEROLE` no superusuario recibe `ADMIN OPTION` automática sobre cada rol que crea (PG16+, medido: `admin=true, inherit=false, set=false`), y `postgres` es el rol más alto disponible. Con esa `ADMIN OPTION` puede concederse `SET` sobre el owner | **ABIERTO por diseño de la plataforma.** En local el forward corre como `supabase_admin` (superusuario), que no genera membresía alguna — medido: 0 filas en `pg_auth_members`. En remoto la separación owner/runtime sólo tiene valor real si el runtime deja de ser `postgres` |
| RR-08 | Funciones y tipos creados en `public` por `postgres` o `supabase_admin` siguen naciendo con `EXECUTE`/`USAGE` para `PUBLIC`. La supresión sólo funciona en su forma **global**, y una entrada global para esos dos roles alcanzaría `auth`, `storage`, `realtime` y `graphql` | **ACEPTADO Y ACOTADO.** Los objetos Uellix los crea `uellix_owner`, que sí tiene las 4 entradas globales. Toda migración que cree una función debe seguir llevando su `REVOKE … FROM PUBLIC`, como ya hacen las 8 existentes |
| RR-09 | `GRANT USAGE ON SCHEMA auth TO uellix_owner` **no es ejecutable en Supabase gestionado**: `auth` pertenece a `supabase_auth_admin` y `postgres` tiene `USAGE` sin `GRANT OPTION`. Sin ese grant, las 3 funciones `SECURITY DEFINER` que llaman a `auth.uid()` fallan con `permission denied for schema auth` para **todos** los invocantes — es decir, toda la RLS del producto | **ABIERTO — BLOQUEANTE PARA G2 REMOTO.** Detectado en el ensayo desechable, antes de tocar el stack vivo. Requiere intervención del soporte de Supabase, o no transferir el owner de esas 3 funciones |
| RR-10 | `uellix_auditor` no ve ninguna fila: no es owner, no tiene `BYPASSRLS`, y las policies dependen de `auth.uid()`, nulo en una conexión directa | **POR DISEÑO.** Es un auditor de estructura y privilegios, no de datos. Darle visibilidad de filas exigiría `BYPASSRLS` o policies dedicadas — **DP-08** |

### Bloqueadores remotos (sin cambio)

`grounding_0001` sigue bloqueado por G5 P3. G2 formal sigue **sin ejecutar**:
exige el entorno remoto autorizado y las precondiciones humanas de
`docs/ops/gates/G2_PACKAGE.md`. Un ensayo local no es staging y RUN 2 no altera
esa distinción.

### Compatibilidad del runtime: identidad en los entry points (2026-08-02)

Segunda mitad del cutover. Contrato completo en
[`DATABASE_RUNTIME_CUTOVER.md`](DATABASE_RUNTIME_CUTOVER.md) §7–§8.

| ID | Riesgo | Estado |
|---|---|---|
| RC-01 | **El login local estaba roto.** `getCurrentUser()` consultaba `public.users` para descubrir el sujeto autenticado; sin claims eso devuelve cero filas, así que toda sesión válida se reportaba como "no autenticado" | **CERRADO** — el sujeto viene de `supabase.auth.getUser()` (verificado contra GoTrue, no decodificado de la cookie) y el perfil se lee **dentro** de un contexto. Probado contra la base viva en `tests/authenticated-database-context.test.ts` |
| RC-02 | 46 entry points consultaban sin contexto. Como `uellix_app` eso no lanza: devuelve **cero filas**, que es la dirección segura y también la más silenciosa — una pantalla vacía es indistinguible de "no hay datos" | **CERRADO** — capa regex: **80 de los 93** entry points de `app/**` que alcanzan `db/client.ts` abren contexto; los **13** restantes están en allowlist con motivo (la cifra 82/11 que figuró aquí era anterior a la última ampliación de la allowlist). Capa AST (cierre 2026-08-02): **117 módulos servidor verificados** (`app/**` + `components/**`, incl. componentes de servidor y módulos `'use server'`), **95** alcanzan la BD, **82 contextualizados + 13 allowlist, 0 sin guardia**, con inventario versionado (`tests/database-entrypoint-inventory.json`) y 10 fixtures mutantes — ver B1/B2 abajo |
| RC-03 | Una transacción abierta durante la llamada al modelo ataría una conexión del pool durante segundos | **CERRADO** — las cinco acciones de Stella se dividen en contexto-de-lectura / llamada externa / contexto-de-escritura. La ruta PDF cierra antes de `renderToBuffer()` |
| RC-04 | Un `redirect()` dentro del contexto haría ROLLBACK de la escritura que acaba de hacerse; un `redirect()` dentro de un `try/catch` de traducción de errores se tragaría en un banner genérico | **CERRADO** — todos los `redirect()` quedaron fuera del callback, y la comprobación de auth se movió antes del `try` en las acciones que traducen errores a `?error=` |
| RC-05 | **Alta autoservicio de organización** y **aceptar invitación** escriben filas que `orgs_insert_super_admin` y `members_insert_admin` niegan a un usuario ordinario. Funcionaban sólo por el bypass de `postgres` | **ABIERTO POR DISEÑO — sin bypass.** Fallan cerrado dentro de un contexto propio. Requiere una policy acotada que exprese "invitación válida" en la base, o una identidad técnica de bootstrap. **Decisión de privilegio, no tomada en esta unidad** |
| RC-06 | **Webhook de Stripe.** No hay sesión; la organización se localiza por `stripe_customer_id`, no por membresía. Sin contexto sus lecturas devuelven cero filas y sus UPDATE afectan cero filas — Stripe recibiría 200 y el cambio de suscripción se perdería para siempre | **ABIERTO POR DISEÑO — sin bypass.** El handler rechaza con **503** después de verificar la firma, para que Stripe reintente con backoff y el operador vea el fallo. Pedir prestada la identidad de un admin habría atribuido una mutación de facturación a quien no la hizo. Requiere identidad técnica de webhook con grant estrecho |
| RC-07 | **Verificación pública por hash.** El hash es una capacidad, pero estaba implementada sólo en la aplicación (`WHERE verification_hash = …`); no hay policy de SELECT anónima sobre `sroi_reports` | **ABIERTO POR DISEÑO.** Devuelve 404. Requiere una policy de capacidad: reportes `locked`, por hash, legibles sin claims |
| RC-08 | **Captura de lead público.** `marketing_leads` era la única tabla de `public` cuyas policies nombraban roles (`TO anon` / `TO authenticated`). Desde `stella_0005c` también las 3 policies `INSERT` append-only nombran rol (`TO uellix_app`); la distribución medida es **101 `{public}` + 3 `{uellix_app}` + 2 `{authenticated}` + 1 `{anon}` = 107**. La cláusula `TO` se contrasta con el **rol de base**, no con el `role` de las claims, y `uellix_app` no es miembro de `anon`/`authenticated` — medido: `pg_has_role` = false en ambos | **ABIERTO POR DISEÑO.** Falla cerrado. Se arregla con una policy INSERT para `{public}` sobre esa tabla. **No** dando a `uellix_app` membresía en `authenticated`: eso le entregaría todos los grants de ese rol en toda la base |
| RC-09 | `lib/auth/identity.ts` y `lib/auth/session.ts` memoizan con `cache()` de React, cuyo alcance por petición es una garantía del App Router | **ACEPTADO Y ACOTADO.** Si no memoiza, el efecto es coste (un viaje extra a GoTrue y una transacción corta extra), nunca privilegio: `resolveRequestPrincipal()` detecta un contexto ya abierto para el mismo sujeto y lee a través de él en vez de anidar, así que la ausencia de memoización tampoco puede provocar un `DB_IDENTITY_NESTED_MISMATCH` |
| RC-10 | Dos APIs de FX de terceros (`datos.gov.co`, `api.frankfurter.app`) son alcanzables desde rutas de **escritura** que corren dentro de una transacción de identidad. Un upstream colgado inmovilizaría una conexión del pool con transacción abierta | **MITIGADO, NO ELIMINADO.** Ambas llamadas pasan a tener `AbortSignal.timeout(8s)`, que acota la ventana de 'indefinida' a 8 segundos. Eliminarla del todo exige sacar la resolución de tipo de cambio del camino de escritura de inversiones y proxies — un cambio de diseño con su propia revisión, **no tomado en esta unidad** |
| RC-11 | La opción `organizationId` de `withOrganizationDatabaseContext` valida un id suministrado por el llamador contra las membresías, pero **ningún** sitio de producción la usa todavía: todos leen `ctx.organization.id` | **ACEPTADO.** No es un defecto; la maquinaria simplemente aún no soporta carga, así que su corrección demuestra menos de lo que aparenta. Se mantiene probada para el día en que un endpoint acepte el id por parámetro |
| RC-12 | IDOR **preexistente** en `createOrganizationFinancialProxy`: `sourceId` viene del cliente y no se contrasta contra la organización, a diferencia de sus dos funciones hermanas. RLS no lo cierra — la fila nueva lleva la organización de la sesión, así que la policy de INSERT pasa, y PostgreSQL evalúa la integridad referencial de la FK con privilegios de sistema | **CERRADO (2026-08-02, cierre de compatibilidad).** `requireUsableProxySource()` en `lib/pipeline/proxies.ts`: la fuente debe ser de la organización de la sesión o global (`organization_id IS NULL`) y activa; inexistente y ajena producen el **mismo** error (`'Source not found'`), sin eco de existencia cross-org. El mismo gate cubre `updateOrganizationFinancialProxy` cuando el parcial trae `sourceId`. Probado en `tests/proxies.service.test.ts` (propia ✓, global activa ✓, global archivada ✗, ajena ✗, inexistente indistinguible, `organizationId` del cliente descartado, la fila insertada conserva la organización de la sesión) |
| RC-13 | La lectura de `marketing_leads` por un super admin (`super_admins_read_marketing_leads`) es igualmente `TO authenticated`, así que está tan muerta como la escritura | **ABIERTO POR DISEÑO**, mismo cierre que RC-08: una policy escrita contra el rol que de verdad conecta |
| RC-14 | En aceptar-invitación el muro que se choca primero no es `members_insert_admin` sino `invitations_select_member` (`organization_id = ANY(current_user_org_ids())`): el invitado no puede ni **leer** su propia invitación, así que `acceptInvitation` falla con `'Invalid invitation'` antes de llegar al INSERT | **ABIERTO POR DISEÑO**, precisión sobre RC-05. La policy de capacidad que haga falta tiene que cubrir el SELECT, no sólo el INSERT |

### Cierre de compatibilidad del runtime (2026-08-02, reauditoría)

| ID | Hallazgo | Estado |
|---|---|---|
| B1 | `OutcomeAllocationWrapper` consultaba `funders` como componente de servidor **durante el streaming del render**, fuera del contexto que la página ya había cerrado; bajo RLS devolvía cero filas y desaparecía en silencio | **CERRADO** — los funders se cargan dentro del `runWithOrganizationAccess` de `outcomes/page.tsx` (la transacción cierra antes del render) y llegan por props; con cero funders el estado vacío es explícito. Regresión cubierta: la forma antigua vive como fixture (`LeakyWidget`) y hace fallar el escáner |
| B2 | El escáner estructural (regex) no veía componentes JSX de servidor ni 10 formas de acceso indirecto (alias, namespace, import dinámico, helper transitivo, reexport, wrapper decorativo, wrapper condicional, módulo no canónico con driver propio, `db` reasignado) | **CERRADO** — escáner AST + grafo de imports (`tests/helpers/entrypoint-scanner.ts`) con peligro **por export**, cobertura de `app/**` + `components/**`, exigencia de que cada uso esté **dentro del argumento** de un opener (mata wrappers decorativos/condicionales), 10 fixtures mutantes que fallan si el escáner deja de verlos, e inventario versionado con comparación semántica (no un conteo fijo) |
| M1 | Policies `INSERT` de `audit_logs`/`stella_interactions` `TO PUBLIC` + grants `INSERT` preexistentes de `authenticated` ⇒ escritura directa por PostgREST con JWT válido; `service_role` (BYPASSRLS local) sólo estaba contenido por el grant, que existía | **CERRADO** — `stella_0005c`: policies `TO uellix_app`, `REVOKE INSERT` a `authenticated`/`service_role`, actor ligado a `auth.uid()` sin rama NULL. Verificado por catálogo, `has_table_privilege` y sonda directa como `authenticated` con claims válidos (denegada) |
| M2 | `tests/integration/_guard.ts` dependía de `DATABASE_URL` (inerte desde el split por capacidad) y la suite era inejecutable | **CERRADO** — guard y setup resuelven `UELLIX_RUNTIME_DATABASE_URL` (rol `uellix_app` verificado, loopback:56322, sin fallback); fixtures migrados a la ruta owner (`_owner.ts`); **49/49 en verde** en local. Hallazgo colateral cerrado: `stella_0005d` (Storage definer sin `USAGE`) |
| M4 | El gate de Stripe no tenía prueba simétrica: encender `WEBHOOK_DATABASE_IDENTITY_AVAILABLE` sin identidad técnica no hacía fallar nada | **CERRADO** — `tests/stripe-webhook-route.test.ts`: firma inválida → 400 con **cero** acceso a BD; firma válida + capability bloqueada → **503** (reintentable) con cero acceso y cero escritura; la constante está fijada a `false` y una identidad técnica introducida a medias también rompe la suite |
| M5 | = RC-12 (IDOR de `sourceId`) | **CERRADO** — ver RC-12 arriba |

**Riesgos locales residuales tras el cierre (revisión adversarial 2026-08-02, 0 BLOCKER / 0 MAJOR).** La revisión independiente confirmó los siete cierres y dejó cinco MINOR; los sustantivos quedan registrados:

- **RR-AST-1 (punto ciego en la capa de identidad).** El escáner AST exime por completo los tres módulos de `contextModulePaths` (`lib/auth/session.ts`, `lib/auth/database-context.ts`, `db/identity-context.ts`) y, al vivir bajo `lib/`/`db/`, tampoco se revisan como entry points. Una consulta **nueva sin contexto** añadida a uno de esos tres archivos no se marcaría estructuralmente. Superficie: exactamente tres archivos, los más escrutados del cambio. Mitigación: las suites vivas `authenticated-database-context` y `database-runtime-rls` los ejercen; `tests/database-runtime-entrypoints.test.ts` fija que esa mitigación exista.
- **RR-AST-2 (forma 11).** Una función tainted **pasada por referencia** a un helper de orden superior que la invoca (`run(listX)`) no se clasifica como uso. No es ninguna de las diez formas que el escáner declara cerrar; se registra como límite conocido, no silencioso, y está anclado en el test.
- **RR-AST-3 (driver fijo).** La forma 9 sólo dispara ante una lista de especificadores de driver; se amplió de 3 a 11 (postgres-js, node-postgres, neon, vercel, mysql…), pero sigue siendo una lista. Un driver nuevo fuera de ella no se marcaría como raíz de BD.
- **RR-AST-4 (`'use server'` fuera de los directorios revisados).** Registrado el 2026-08-03, en el preflight del diseño de capacidades. El escáner separa `scanDirs` (`app`, `components`, `lib`, `db` — lo que se parsea para construir el grafo de imports) de `checkedDirs` (`app`, `components` — lo que se juzga como entry point). En `listCheckedModules()` **todas** las ramas están condicionadas a `isUnderCheckedDir()`, incluida la de `isUseServer`. Consecuencia: un módulo con directiva `'use server'` bajo `lib/**` (o `db/**`) participaría en el grafo y propagaría *danger* hacia sus consumidores, pero **nunca se clasificaría** como entry point — es decir, una server action invocable desde el cliente que el escáner no mira. **Instancias reales: cero.** Verificado el 2026-08-03 sobre el árbol completo: `app/**` tiene 93 directivas `'use server'` reales, `components/**` 0 y `lib/**` 0 (la única coincidencia textual en `lib/` es la advertencia en prosa de `lib/stella/advisor/run-contextual-advisor.ts`, que precisamente prohíbe exportar esa función desde un módulo `'use server'`; no es una directiva). Severidad: MINOR — riesgo estructural con población vacía, no un hallazgo. Mitigación disponible si la población deja de ser cero: mover la rama `isUseServer` de `listCheckedModules()` fuera de la guarda `isUnderCheckedDir`, de modo que cualquier `'use server'` en cualquier `scanDir` quede revisado con independencia de su ubicación. No se aplica en esta unidad: el cambio altera el escáner y su inventario versionado, y esta unidad es de **diseño**, no de implementación. Anclaje: mientras `lib/**` tenga cero directivas, el riesgo no tiene superficie explotable.

---

## Diseño de capacidades públicas (2026-08-03)

**Unidad de DISEÑO. Nada aplicado, ninguna capacidad habilitada, cero acceso
remoto, cero grounding, cero G2 formal.** Fuente:
[`DATABASE_CAPABILITY_MODEL.md`](DATABASE_CAPABILITY_MODEL.md) y los cinco
documentos de `capabilities/`.

### Relación con hallazgos ya registrados

Dos entradas previas de este registro describen exactamente lo que este diseño
tenía que resolver, y conviene enlazarlas en lugar de duplicarlas:

* **RC-14** ya había precisado que en la aceptación de invitación el muro que
  se choca primero es `invitations_select_member`, no `members_insert_admin`, y
  que *"la policy de capacidad que haga falta tiene que cubrir el SELECT, no
  sólo el INSERT"*. CAP-01 lo cubre: `cap_invitation_select_invitations` +
  `cap_invitation_update_invitations` + `cap_invitation_insert_members`.
* **RC-13** ya había registrado que `super_admins_read_marketing_leads` está
  tan muerta como la escritura. CAP-04 **no** la repara y explica por qué: es
  una capacidad de lectura administrativa con su propio modelo de amenaza, y
  agruparla con la captura pública es precisamente lo que la campaña prohíbe.
  Se renombra aquí como **RR-CAP-6** sólo para el índice del modelo; el
  hallazgo es RC-13 y sigue **ABIERTO POR DISEÑO**.

### Riesgos del modelo (no de una capacidad concreta)

| ID | Riesgo | Severidad | Estado |
|---|---|---|---|
| **RR-CAP-0** | **Reformulado tras el dry run (2026-08-03).** Ya **no** dice «todo es SQL leído, no SQL corrido»: eso era falso desde el primer ensayo, y la fila lo siguió afirmando mientras la prosa al pie de esta misma sección decía lo contrario. Lo que queda abierto es más pequeño y más preciso: **el dry run no es Supabase gestionado**. Los cinco paquetes se aplicaron dos veces, se probaron, se revirtieron y se reaplicaron en un contenedor desechable **sin red**, sembrado desde un volcado *schema-only* obtenido por lectura. Nada se ha aplicado al stack vivo ni a remoto | **MEDIA** — el ensayo existe; la equivalencia con el entorno gestionado no | ABIERTO (reducido) |
| **RR-CAP-10** | Cualquier `organization_admin` puede escribir `stella_monthly_quota` y `stella_plan_label` por el ORM: `stella_0004` §6b concede `UPDATE` a nivel de tabla sobre `public.organizations` a `uellix_writer`, `uellix_app` hereda, y `orgs_update_admin_or_super` es `{public}` sin predicado de columna. **Preexistente**: CAP-03 no lo añade ni lo cierra | **MAJOR** | **CERRADO 2026-08-04** por `db/prepared/stella_0011_organization_column_acl.sql` (+ `stella_0011_rollback.sql`). Hasta ese momento fue la **PRECONDICIÓN BLOQUEANTE DE CAP-03**: su proposición central («la cuota sólo se mueve por un evento firmado») era falsa mientras la vía existiera, y *degrada* CAP-05, cuyo argumento «el bootstrap no elige plan» era cierto en el paquete y falso en el sistema porque el admin recién creado podía escribir la cuota por el ORM al minuto siguiente. **Una policy no podía sustituir a esto, y sigue sin poder:** RLS evalúa la fila, no la lista de columnas de la sentencia, y no existe forma de comparar `OLD` con `NEW` dentro de un `USING`/`WITH CHECK` para detectar «esta columna cambió». El control de columna es ACL o es trigger; no es policy. **La reparación es la que el registro prescribía, y se ejecutó entera:** (1) `REVOKE UPDATE` de tabla completa sobre `public.organizations` **a `authenticated`, `uellix_writer`, `anon` y `PUBLIC`**; (2) `GRANT UPDATE (…)` por columnas a `uellix_writer` **y a `authenticated`** — el segundo es el que se olvida y es el que usa PostgREST. **Las ocho columnas están derivadas del código, no inventadas:** son exactamente las de los cinco `db.update(organizations)` del repositorio (`base_currency, brand_color, country, logo_url, onboarding_completed, sector, updated_at, white_label_enabled`). **Tres decisiones que no son contabilidad y quedan escritas:** `name`/`slug`/`legal_name` resultan ser **sólo de INSERT** en la práctica y salen del grant, lo que deja **RR-CAP-02-D sin objeto** mientras nadie añada renombrado; `status` es una decisión de plataforma que llegaba por la misma conexión que la de cualquier tenant —una organización suspendida podía reactivarse a sí misma— y **pasa al definer**; `white_label_enabled` **se queda**, porque nada en el código lo condiciona al plan y hoy es marca y no *entitlement* (ver **RR-CAP-10-B**). **Por qué hizo falta además un definer:** un ACL no distingue un `super_admin` de plataforma de un `organization_admin` —ambos llegan como `uellix_app`—, así que revocar la cuota rompería `lib/admin/stella-services.ts` y `lib/admin/organizations.ts`. El camino legítimo se mueve a `uellix_capability.admin_set_stella_service` y `admin_set_organization_status`, propiedad de `uellix_cap_platform`, con **doble cierre**: el cuerpo exige `current_user_is_super_admin()` y `cap_platform_only_super_admin` (RESTRICTIVE) exige lo mismo del **llamante**, de modo que un cuerpo reescrito sigue sin poder mover una cuota. **Evidencia:** `RR10-1..RR10-13` vivas en el *dry run* (cuota, plan, `status` y los tres `stripe_*` refusados a `uellix_writer` y a `authenticated`; columna permitida + prohibida en la misma sentencia refusada; las dos actualizaciones legítimas verdes; el admin de tenant refusado por el definer; el super_admin de plataforma admitido y auditado) y mutaciones `R-25..R-31`, `P-23..P-31`. **CAP-03 queda desbloqueado**: su proposición central es ahora cierta en el sistema, no sólo en el paquete . **Actualización 2026-08-04 (tarde):** el cierre declarado en `stella_0011` era **incompleto y la reauditoría final lo devolvió como `BLOCKED_QUOTA`**. La frontera se delegó en `public.current_user_is_super_admin()`, y ese predicado lee una sola columna que el runtime podía escribir (**RR-CAP-15**). Con `stella_0012` aplicado, y sólo con él, la proposición «la cuota sólo se mueve por un evento firmado o por un super_admin de plataforma» es cierta. **Cerrado también en código:** `lib/admin/stella-services.ts` y `lib/admin/organizations.ts` ya no emiten `db.update(organizations)`; llaman a las dos funciones del *definer* a través de `lib/admin/organization-administration.ts` |
| **RR-CAP-1** | `uellix_owner` puede `SET ROLE` a los cinco roles de capacidad — necesario para transferirles la propiedad de las funciones. Quien controle el camino de DDL controla las cinco capacidades | MINOR | ACEPTADO (ya controlaba todo lo demás) |
| **RR-CAP-2** | Las policies de los roles de capacidad son amplias por construcción (`USING (true)`): una policy no ve los argumentos de la función que la invoca. La estrechez vive en el cuerpo; la contención, en los grants por columna | MINOR | ACEPTADO Y DOCUMENTADO |
| **RR-CAP-3** | El token de invitación en claro cruza de la aplicación a la base. Como parámetro ligado `pg_stat_statements` lo normaliza, pero `log_statement='all'` lo registraría | MINOR | MITIGACIÓN OPERATIVA (comprobación de runbook) |
| **RR-CAP-4** | Cinco roles nuevos son cinco cosas más que auditar | NIT | MITIGADO por `tests/capability-isolation.test.ts` |
| **RR-CAP-5** | Supabase gestionado puede no admitir `uellix_stripe` (rol `LOGIN` nuevo), por las mismas tres limitaciones que bloquearon `stella_0004` en remoto | **ABIERTO — NO VERIFICABLE** en esta unidad (remoto prohibido) | ABIERTO |
| **RR-CAP-6** | = RC-13 | — | ver arriba |
| **RR-CAP-7** | `PUBLIC` tiene `USAGE` sobre el esquema `public` (ACL medida: entrada `=U/pg_database_owner`), así que **todo rol nuevo lo hereda** y los ACL de PostgreSQL son aditivos: no hay *deny* por rol. Un borrador de este diseño afirmaba que `uellix_stripe` "no puede ni nombrar `public.projects`" — **era falso** y se corrigió. La propiedad que sí se afirma y se verifica es **cero privilegio sobre las 38 tablas en los 4 modos DML** | MINOR | ABIERTO como hardening candidato (`REVOKE USAGE ON SCHEMA public FROM PUBLIC` alcanza a roles internos de Supabase; exige análisis propio) |

### Riesgos por capacidad

| ID | Riesgo | Severidad |
|---|---|---|
| **RR-CAP-01-A** | Canal lateral de temporización en CAP-01: el camino "token válido, destinatario distinto" ejecutaba una consulta más que "token inexistente". Mitigado resolviendo el correo del sujeto **antes** de mirar la invitación, de modo que ambos caminos ejecuten el mismo número de sentencias. **No se declara cerrado**: la igualdad exacta no se ha medido y no se puede medir sin ejecutar | MINOR |
| **RR-CAP-01-B** | Si DP-CAP-02 elige mensajes distinguibles, el oráculo de enumeración vuelve. El diseño lo permite pero no lo recomienda | MINOR — depende de decisión humana |
| **RR-CAP-01-C** | El barrido de invitaciones expiradas no forma parte de la capacidad (la función ya no escribe en el camino de rechazo). Higiene, no seguridad | NIT |
| **RR-CAP-02-A** | El hash viaja en la URL `/verify/<hash>`, luego aparece en logs de acceso del proveedor y en el historial. Inherente a que el certificado sea un enlace compartible; lo que desbloquea es material aprobado para publicación | MINOR |
| **RR-CAP-02-B** | La ruta PDF pública queda huérfana hasta DP-CAP-06 y **debe devolver 404 incondicional** mientras tanto. Es la única parte de CAP-02 que exige un cambio de código **antes** de aplicar el paquete | **MAJOR si se aplica sin ese cambio** |
| **RR-CAP-02-C** | Los reportes ya bloqueados **no** obtienen disclosure automáticamente: los certificados emitidos antes dejan de verificar hasta que alguien los apruebe uno a uno. Correcto (nadie autorizó publicarlos) pero es una regresión visible que hay que comunicar | MINOR |
| **RR-CAP-02-D** | `organizations.name` se publica si el booleano está en `true`, y es editable por la organización. La aprobación es un acto humano que ve el nombre en ese momento; no hay revalidación posterior | NIT |
| **RR-CAP-03-A** | El *lease* de 15 minutos sobre `processing` es una heurística, no una garantía: un worker colgado más de 15 min con la transacción abierta permitiría reprocesar. `idle_in_transaction_session_timeout = 15s` lo hace muy improbable | MINOR |
| **RR-CAP-03-B** | El `catch` genérico del handler actual registra el objeto de error completo. Debe pasar a `error.name` **antes** de habilitar, como ya hace la ruta de leads | MINOR — **precondición de habilitación** |
| **RR-CAP-03-C** | DP-CAP-07 sin resolver: `stripe_webhook_events` crece sin límite. Coste, no seguridad. La capacidad **no tiene `DELETE`**, así que no puede borrar la prueba de lo que hizo | NIT |
| **RR-CAP-04-A** | Llenado distribuido de `marketing_leads` dentro del límite global. Mitigación operativa (alerta sobre el contador diario) | MINOR |
| **RR-CAP-04-B** | Sin CAPTCHA, un bot competente pasa el honeypot. Añadirlo es decisión de producto y de privacidad (implica un tercero) | MINOR |
| **RR-CAP-04-C** | `consent_version` anulable hasta DP-CAP-09: los leads se guardan sin registro de consentimiento, igual que hoy. El diseño no empeora nada, pero tampoco lo arregla | MINOR |
| **RR-CAP-05-A** | Si DP-CAP-12 abre el alta más allá de la allowlist, el perfil de abuso cambia por completo y **este diseño no basta**: haría falta verificación de correo, CAPTCHA y cuarentena. Es la dependencia más fuerte de una capacidad respecto a una decisión de producto | **ALTA si DP-CAP-12 cambia** |
| **RR-CAP-05-B** | El error de slug es distinguible **a propósito** (el espacio de slugs es público por diseño). Debe revisarse si DP-CAP-12 cambia el modelo de URL | NIT |
| **RR-CAP-05-C** | La "configuración inicial" no es una tabla aparte: en este esquema la configuración de una organización **son sus propias columnas**, y toda la que la capacidad no nombra toma el `DEFAULT`. Ese es el mecanismo por el que no puede elegir plan ni cuota | CERRADO — documentado |
| **RR-CAP-05-D** | `syncUserProfile` ocurre **fuera** de la RPC. Si falla, el `SELECT` sobre `public.users` no encuentra al sujeto y el bootstrap devuelve error uniforme: fail-closed y correcto, pero opaco para un usuario legítimo. El endpoint debe llamarlo y comprobar su resultado **antes** de la RPC | MINOR |

### Riesgos añadidos tras la revisión adversarial y el dry run (2026-08-03)

Inventario completo de hallazgos y su cierre:
[`capabilities/ADVERSARIAL_FINDINGS.md`](capabilities/ADVERSARIAL_FINDINGS.md).
**10 BLOCKER y 7 MAJOR cerrados**, todos verificados por ejecución en un
contenedor desechable sin red. Ninguna severidad se rebajó.

| ID | Riesgo | Severidad | Estado |
|---|---|---|---|
| **RR-CAP-02-E** | El tiempo de `verify_report` **no está igualado**. Un hash inexistente se corta en el índice único de `verification_hash` y nunca sondea `report_public_disclosures`; uno existente pero no publicado sí. El *resultado* es indistinguible por construcción del `JOIN`; la *latencia* no. Un borrador afirmaba lo contrario y se corrigió | MINOR | ABIERTO — registrado, no mitigado. Mismo trato que RR-CAP-01-A |
| **RR-CAP-02-F** | CAP-02 §10 afirmaba que crear y revocar una disclosure se auditan. **No hay trigger, ni inserción en `audit_logs`, ni protección append-only sobre `report_public_disclosures`**: un admin puede publicar, revocar y volver a publicar sin dejar rastro. La afirmación descansaba en código de aplicación inexistente | MINOR | **CERRADO 2026-08-04**. **Qué quedaba sin rastro, exactamente, y ya no:** la decision humana de publicar y la de revocar, el contenido de `public_summary` —texto libre redactado por una persona— y el estado de los **seis** flags `show_*` (`show_organization_name`, `show_report_title`, `show_headline_ratio`, `show_totals`, `show_issued_on`, `show_report_variant`) en el momento de publicar. **Qué bloqueaba y qué no:** no bloquea *aplicar* `stella_0007` —el paquete era correcto y su rollback retiene la tabla precisamente porque cada fila es una decisión humana— pero sí **bloquea habilitar** la verificación pública de cara al exterior, porque el argumento de auditabilidad que sostiene el producto no existía hasta que la publicación dejara rastro; ese bloqueo queda levantado. `stella_0007` crea `public.uellix_audit_report_disclosure()` y el trigger `trg_report_disclosure_audit` (`AFTER INSERT OR UPDATE … FOR EACH ROW`) sobre `report_public_disclosures`. **Cuatro transiciones con nombre propio** —`report.disclosure.published` / `.revoked` / `.reinstated` / `.visibility_changed`— porque colapsarlas en «updated» deja «cuántos certificados se retiraron» sin respuesta. Cada evento registra organización (derivada del reporte, nunca aseverada), reporte, actor, operación, estado anterior y posterior relevantes, `created_at` del servidor y un `requestId` cuando el llamante lo puso en `app.request_id`. **Atómico por construcción:** al ser `AFTER` en la misma transacción, un fallo de la escritura de auditoría revierte la publicación con él — probado activamente en `RR02F-6`, que hace imposible el `INSERT` y comprueba que el cambio **no** queda. **No es `SECURITY DEFINER`, a propósito:** corre con los privilegios del llamante y bajo `audit_logs_insert_member_or_admin`, así que `actor_user_id = auth.uid()` lo impone RLS y no la función. **Qué NO se guarda:** `public_summary` viaja como **digest SHA-256 y longitud**, nunca como texto; no hay payload, ni evidencia, ni prompts, ni tokens. El digest responde igual a la pregunta que el rastro existe para responder, que la fila sola no puede porque `public_summary` es actualizable. **Append-only en las dos tablas:** `audit_logs` ya lo era y ningún rol de runtime tiene `UPDATE`/`DELETE`; `report_public_disclosures` lo es ahora vía `trg_report_disclosures_append_only` y `trg_report_disclosures_no_truncate`, reutilizando `public.uellix_forbid_mutation`, de modo que una decisión publicada no la borra **ni el propietario de la tabla**. El rollback conserva esos dos triggers y sólo retira el de auditoría. **Se reutilizó la infraestructura existente, no se creó una segunda.** **Evidencia:** `RR02F-1..RR02F-9` vivas y mutaciones `R-01..R-15`, `P-01..P-02`. Con esto **la verificación pública deja de estar bloqueada por auditabilidad**; los demás gates externos siguen donde estaban |
| **RR-CAP-02-G** | `capability_verification_hits` no lo lee **nada**: ninguna función y ningún grant lo exponen al producto, y el rollback lo elimina. La pregunta que el diseño dice poder responder —«¿cuántas veces se verificó este certificado?»— no está implementada | NIT | ABIERTO — el `COMMENT` de la tabla dice ahora «recogido, no expuesto» |
| **RR-CAP-02-H** | `verify_report` devuelve `public_summary` y `disclosure_version` **sin booleano de visibilidad**, mientras la cabecera del paquete afirma que «cada campo visible es un booleano explícito que alguien tuvo que activar». Es la misma asimetría que la ronda 2 corrigió para `issued_on` y `report_variant`, aplicada al único campo de texto libre que queda; y `public_summary` está en el `GRANT UPDATE` del runtime, así que una edición de un admin es pública al instante sin acto de aprobación separado | **MAJOR** | **ABIERTO — no corregido aquí, y la razón importa.** Cerrarlo es añadir un séptimo booleano `show_public_summary`, lo que cambia el invariante de **seis** flags que está escrito en la tabla, en las policies, en los grants, en `DISCLOSURE_FLAGS`, en las postcondiciones y en cinco documentos. Esta unidad tenía instrucción explícita de **no rediseñar CAP-01..CAP-05**, y RR-CAP-02-H no es uno de los cinco riesgos que se le encargó cerrar. Hallado por revisión adversarial (revisor B). Argumento contrario, para que quien decida lo tenga: `public_summary` es por definición el texto que una persona escribió **para publicarlo**, así que no es una fuga sino el objeto; lo discutible es que su edición posterior no requiera una segunda aprobación. Decisión de producto |
| **RR-CAP-02-I** | Los cuatro triggers de la cadena de auditoría se crean con `tgenabled = 'O'`, así que `SET session_replication_role = 'replica'` los suprime: publicar, revocar y borrar quedan silenciosos | MINOR | **ABIERTO — mismo residual aceptado que MIN-06 para `stella_0002b`, extendido ahora a una propiedad nueva y por eso re-registrado en vez de heredado en silencio.** Hallado por revisión adversarial (revisor B), que además señaló que la postcondición fijaba `tgenabled = 'O'` y por tanto **hacía abortar el paquete sobre una base que un operador hubiera endurecido con `ENABLE ALWAYS`**. Eso sí está corregido: las postcondiciones aceptan ahora `IN ('O','A')`, de modo que el estado endurecido es al menos aplicable. Activarlo por defecto es una decisión operativa con impacto en replicación |
| **RR-CAP-12** | **Una suite verde no es una suite que detecte.** `tests/capability-isolation.test.ts` pasaba 220/220 con **22 mutaciones de seguridad vivas**: reapuntar el `TO` de una policy `RESTRICTIVE`, sustituir un `USING` por `true`, vaciar un `WITH CHECK`, fusionar `GRANT INSERT (…), SELECT`, devolver el `match_kind` `organization` a CAP-03, mover la reclamación de la clave de CAP-05 tras la creación de la organización. Ninguna alteraba un conteo, y la suite comprobaba conteos | **MAJOR** | **MITIGADO, no cerrado** (2026-08-03). El contrato es ahora una función pura sobre el texto (`tests/helpers/capability-gates.ts`) que `tests/capability-mutation.test.ts` ejecuta contra 67 copias rotas, exigiendo además que refuse el **gate correcto** y no uno cualquiera. Medido: 22/22 sobreviven a la suite antigua, **0/67 a los gates nuevos**, 67/67 restauradas por SHA. **Por qué no está cerrado:** «0/67» es acuerdo entre dos ficheros escritos juntos —el catálogo y los gates—, no cobertura del diseño. Dos revisores adversariales de solo lectura encontraron **once escapes reales en una sesión** (propiedad de función, RLS sin habilitar, un segundo `ALTER ROLE`, `INHERIT`, policies y grants dentro de un rollback, un séptimo booleano de publicación, el rol fundador, los flags que `verify_report` no leía, la denylist de slugs, el privilegio del `REVOKE`, y borrar una policy ajena); todos están ahora catalogados y en rojo, pero ninguno exigió imaginación y varios eran de la misma clase que mutaciones ya presentes. Residual explícito: **27 de 154 gates no los ejercita ninguna mutación** y están listados en `UNEXERCISED_GATES` para que crecer esa lista sea un acto visible. Actualización 2026-08-04: el catálogo pasa de 67 a **96 mutaciones** con las ocho evasiones confirmadas, catorce equivalencias más y siete mutaciones de la ronda adversarial; ver RR-CAP-12b . **Actualización 2026-08-04 (cierre de riesgos de diseño):** el catálogo pasa de 96 a **181** mutaciones (`R-01..R-33` para las propiedades nuevas, `P-01..P-33` para gates primarios que nunca habían tenido evidencia negativa) y el contrato de 123 a **154** gates. El residual baja de 56 a **27**, y ahora está **clasificado en el propio fichero**: *fail-safe* (el objeto falta y la capacidad deja de funcionar ruidosamente), disponibilidad, conteo, auto-comprobación del arnés, minimización de datos, y *cubierto por un gate hermano con evidencia negativa* —cada línea nombra la mutación que alcanza la misma propiedad—. **Ninguno de los 26 es la única protección de una frontera de autorización**: los que lo eran (`cap05-allowlist`, `cap03-blast-radius`, `cap03-login-identity`, `cap02-locked`, `cap02-revoked`, `cap02-minimal`, `cap02-readonly`, `cap01-status`, `cap01-subject`, `cap01-token-hash`, `cap01-single-membership`, `cap05-single-membership`, `cap05-no-plan`, `cap05-slug-atomic`, `cap04-retire-dead-policies`, `cap04-server-derived`, `cap04-status-constant`, `definer-security`, `definer-select-star`, `policy-command`, `role-crossgrant`, `rollback-role`, `rollback-function`, `rollback-rls`, `rollback-ownership`, `rollback-cascade`) tienen mutación propia desde esta unidad. **Estado: MITIGADO**, no cerrado, y por la misma razón de siempre — sigue siendo acuerdo entre el catálogo y los gates |
| **RR-CAP-12b** | **El lector divergía de PostgreSQL y la divergencia era silenciosa.** Una reauditoría independiente confirmó **ocho grafías** válidas de PostgreSQL que el parser no veía: DDL/DCL dentro de un bloque `DO`; identificadores y *grantees* entre comillas dobles; `GRANT a, b TO c`; `DISABLE ROW LEVEL SECURITY`; un segundo `ALTER ROLE` que revierte atributos seguros; `REASSIGN OWNED`; `CREATE POLICY` con identificadores entrecomillados; y comentarios de bloque **anidados** que cambian el código ejecutable. Ninguna era una propiedad nueva: eran ocho maneras de escribir propiedades ya cubiertas. La causa raíz es una sola — **la ausencia de match se interpretaba como ausencia de riesgo** | **BLOCKER** | **MITIGADO** (2026-08-04). `tests/helpers/sql-structure.ts` se reconstruyó como **lexer + escáner fail-closed**: reglas léxicas de PostgreSQL (identificadores normalizados con y sin comillas, comentarios anidados, `E''`, *dollar quoting* con y sin etiqueta), descenso a los cuerpos ejecutables (`DO`, cuerpos de función, literales de `EXECUTE`) y una violación explícita `unparsed-security-statement` para toda sentencia que **abra** como operación de seguridad y no clasifique. El `EXECUTE` de un `format(…)`, de una variable o de una concatenación **se rechaza**, no se interpreta. Medido: **96/96 mutaciones detectadas, 0 supervivientes, 0 capturadas por un gate ajeno a su propiedad, restauración verificada por SHA**; las 8 evasiones tienen además control negativo independiente a nivel de parser. **Por qué MITIGADO y no CERRADO:** sigue siendo acuerdo entre el catálogo y los gates, y el residual de RR-CAP-12 no cambia de naturaleza — 27 de 154 gates no los ejercita ninguna mutación. Hallazgo propio de esta unidad, además: el inventario de gates se deriva casando `add('<literal>'` en el fuente, así que **un gate con el nombre calculado en tiempo de ejecución es invisible para la comprobación de cobertura**; ocurrió con dos gates y ahora se comprueba desde el otro extremo |
| **RR-CAP-13** | `sroi_calculation_runs` tiene policy permisiva `USING (true)` para `uellix_cap_verification` y **ninguna compañera `RESTRICTIVE`**, a diferencia de `sroi_reports`. Las cifras que publica el certificado (`sroi_ratio`, `total_investment`, `net_social_value`, `currency`) viven en esa tabla, y hoy el único vínculo con `status='locked'` es el `JOIN` del cuerpo de `verify_report` | MINOR | **CERRADO 2026-08-04** en `stella_0007` con dos `RESTRICTIVE` nuevas: `cap_verification_only_published_org` sobre `organizations` y `cap_verification_only_published_run` sobre `sroi_calculation_runs`. **Alcance exacto:** afectaba a las dos tablas cuyo `SELECT` de `uellix_cap_verification` era `USING (true)` sin compañera `RESTRICTIVE` — `public.sroi_calculation_runs` y `public.organizations`. **La severidad estaba subestimada y conviene decirlo:** el ACL de columna acotaba *qué campos*, nunca *qué filas*, así que con `USING (true)` la identidad pública podía enumerar el **nombre de todas las organizaciones de la plataforma** y la **ratio de todos los cálculos jamás ejecutados**, publicados o no — la lista de clientes y los resultados no publicados. El predicado es la misma proposición que hace el `JOIN` de `verify_report` (reporte **bloqueado** con disclosure **viva** apuntando a la fila), movida a donde no se reescribe editando una función; **no se concede un solo `SELECT` adicional** para resolver los joins, porque las subconsultas leen exactamente las columnas ya concedidas. Revocar una disclosure retira organización y cálculo en la misma sentencia que retira el reporte. **Evidencia:** `RR13-1..RR13-7` vivas (una sola organización visible; cero tras revocar todas las disclosures; el cálculo no vinculado nunca aparece; `verify_report` sigue respondiendo) y mutaciones `R-16..R-18` |
| **RR-CAP-14** | CAP-03 no tiene **ninguna** policy `RESTRICTIVE`. `cap_stripe_update_orgs` es `USING (true) WITH CHECK (true)`, de modo que toda la frontera del definer del webhook es el `GRANT UPDATE` de seis columnas | MINOR | **CERRADO 2026-08-04** en `stella_0008` con `cap_stripe_only_claimed_read` (SELECT) y `cap_stripe_only_claimed_org` (UPDATE), ambas `RESTRICTIVE`. **Corrige un razonamiento que el propio CAP-03 §8 sostenía y era falso:** decía que una policy no puede expresar «sólo la organización que casa con este `stripe_customer_id`» porque no ve los argumentos de la función. La premisa es cierta y la conclusión no: **la policy no necesita los argumentos, necesita el evento**. `stripe_begin_event` graba ahora los dos identificadores emitidos por Stripe a los que el evento firmado va dirigido —firma ampliada a cuatro argumentos, la de dos se elimina en el paquete— y la policy exige que exista un evento en `processing`, con dirección no nula, que **coincida** con la de la fila. **Cero update cross-organization** (la fila de B lleva la dirección de B, diga lo que diga el cuerpo), **cero update de organización sin vínculo Stripe** (ambas columnas `NULL` no casan con nada, que es lo que DP-CAP-15 exige), **cero confianza en `client_reference_id`** (sigue sin ser autoridad), y el ancla **no es reescribible**: los dos identificadores están fuera del `GRANT UPDATE` de `stripe_webhook_events` y re-reclamar un `event_id` bajo otra dirección se refusa. La cota de `SELECT` no es decorativa: el cuerpo resuelve la organización **escaneando** la tabla, así que con `USING (true)` el definer leía las columnas de facturación de todos los clientes. **El alcance era cross-organization:** `USING (true)` sobre `public.organizations` significaba que un evento procesado en nombre de la organización A podía, si el cuerpo se reescribía mal, actualizar la fila de la organización B — las seis columnas del `GRANT` acotan *qué* se escribe, nunca *de quién*. **Y no se cierra sólo con ACL de columnas:** ninguna lista de columnas expresa «esta fila corresponde al `stripe_customer_id` del evento»; se cierra atando la fila a la identidad Stripe ya vinculada, que es exactamente lo que hacen las dos policies nuevas. **Es independiente de RR-CAP-10 y conviene no confundirlos:** RR-CAP-10 controla columnas del runtime, RR-CAP-14 controla filas alcanzables por la identidad Stripe; se cerraron con paquetes distintos y policies distintas. **Evidencia:** `RR14-1..RR14-8` vivas y mutaciones `R-19..R-24` |
| **RR-CAP-14-A** | **La base de datos no puede verificar una firma de Stripe.** Esa comprobación vive entera en el manejador Node, así que cualquiera que tenga la credencial de `uellix_stripe` puede reclamar un evento para un `stripe_customer_id` que elija y aplicar un cambio para él. Ninguna policy distingue eso de una entrega genuina: ambas llegan por las mismas tres funciones con los mismos argumentos | **MAJOR** | **ABIERTO — inherente al diseño, no una regresión de RR-CAP-14.** La vía existía antes de esta unidad y sigue existiendo después; **la credencial ES la frontera de confianza**, y CAP-03 lo dice desde su cabecera (se fija fuera de banda y no debe compartirse con ningún otro servicio). Hallado por revisión adversarial (revisor A). **Qué SÍ acota ahora la cota de fila, que es todo lo que falta para eso:** una organización sin vínculo Stripe es inalcanzable; un cuerpo reescrito no alcanza una fila a la que ningún evento reclamado va dirigido; y una transacción que aplica un evento no alcanza la organización de otro evento en vuelo. **Mitigación real disponible y no aplicada aquí:** rotar la credencial es un acto operativo (DP-CAP-07), y una comprobación de firma en la base exigiría custodiar el secreto de Stripe en PostgreSQL, que es peor. Se registra para que nadie lea la cota de fila como si fuera autenticación |
| **RR-CAP-10-A** | **Aplicar `stella_0011` sin un cambio de código previo rompe dos pantallas de administración de plataforma.** `lib/admin/stella-services.ts` y `lib/admin/organizations.ts` siguen emitiendo `db.update(organizations).set({…})` directamente; en cuanto el paquete se aplique fallarán con 42501 hasta que se reescriban como `SELECT uellix_capability.admin_set_stella_service(…)` y `admin_set_organization_status(…)`. Ambas funciones ya escriben la fila de auditoría, así que el `logAuditAction` de esos dos puntos pasa a ser **duplicado** y hay que retirarlo en la misma edición | **MAJOR si se aplica sin ese cambio** | **ABIERTO — es una restricción de ORDEN, no un defecto.** Mismo trato que RR-CAP-02-B con la ruta PDF pública. Esta unidad **no** hace el cambio de código: los paquetes están preparados y no aplicados, y publicar código que llama a una función inexistente rompería el producto en marcha. Registrado aquí para que no se descubra en una ventana de mantenimiento . **CERRADO 2026-08-04 (tarde).** Los dos *call sites* llaman ahora a `uellix_capability.admin_set_stella_service` y `admin_set_organization_status` mediante un envoltorio tipado con validación de UUID, refusal saneado y **sin *fallback* ni *feature flag*** — un *fallback* mantendría viva en el árbol la sentencia que RR-CAP-10 existe para quitar, y una bandera con el camino antiguo por defecto significaría que la frontera está apagada en producción mientras las pruebas ejercitan la nueva. El `logAuditAction` duplicado se retiró: el *definer* escribe el cambio y su fila de auditoría en una transacción, y `setOrganizationStatus` **gana** una auditoría que nunca tuvo. **El riesgo no desaparece, cambia de forma:** deja de ser «el código debe cambiar antes» y pasa a ser **despliegue acoplado** — `stella_0011` + `stella_0012` + `lib/admin/**` van juntos. Código sin paquetes: 42883. Paquetes sin código: 42501 |
| **RR-CAP-10-B** | `white_label_enabled` permanece en el `GRANT UPDATE` del runtime, de modo que un `organization_admin` puede activarse el marcado blanco. Es un flag con **forma** de *entitlement* y RR-CAP-10 nombra «feature flags» entre lo que un admin de organización no debe mover | MINOR | **ABIERTO — decisión explícita, no un olvido.** Nada en el repositorio condiciona el marcado blanco al plan: `app/actions/organization.ts` comprueba el **rol** y ningún plan. Hoy es marca, no derecho, y revocarlo rompería la pantalla de ajustes para proteger una frontera que todavía no existe. **El día que el marcado blanco se condicione al plan, la columna tiene que moverse al definer de plataforma**, y esa es la condición que hace de esto un riesgo y no una nota |
| **RR-CAP-10-C** | `service_role` conserva `UPDATE` de tabla completa sobre `public.organizations` **y** `BYPASSRLS`. Cualquiera con la *service key* de Supabase mueve una cuota saltándose RLS por completo y **sin escribir fila de auditoría**: `service_role` sólo tiene `SELECT` sobre `audit_logs` | **MAJOR** | **ABIERTO — fuera de alcance, declarado.** Estrechar `service_role` alcanza roles internos de Supabase y es territorio de RR-CAP-7; refusar aplicar `stella_0011` por ello sería refusar cerrar RR-CAP-10. Hallado por revisión adversarial (revisor A), que además señaló con razón que el `COMMENT ON COLUMN` que el paquete escribía en el esquema afirmaba lo contrario. **Corregido en dos sitios:** el `COMMENT` nombra ahora la vía abierta, y `stella_0011` emite un `RAISE WARNING` en sus postcondiciones cuando `service_role` retiene el privilegio, de modo que el residual aparece en la terminal del operador y no sólo en un comentario de cabecera . **Ampliado 2026-08-04 (tarde):** `service_role` conserva también `UPDATE` de tabla completa sobre `public.users`, así que la *service key* puede escribir `is_super_admin` directamente y saltarse RR-CAP-15 igual que salta RR-CAP-10. `stella_0012` emite un `RAISE WARNING` propio cuando lo detecta. Sigue fuera de alcance por la misma razón (RR-CAP-7) |
| **RR-CAP-15** | **Autoescalada a super-admin de plataforma en una sentencia.** `GRANT SELECT,INSERT,UPDATE ON TABLE public.users` a `authenticated` **y** a `uellix_writer` es de tabla completa; `users_update_own` no tiene cláusula `TO` (⇒ `TO PUBLIC`) y su predicado es `id = auth.uid()` en `USING` y en `WITH CHECK`; `public.users` no tiene ningún trigger; `is_super_admin boolean DEFAULT false NOT NULL` no tiene `CHECK`. Cualquier usuario autenticado podía ejecutar `UPDATE public.users SET is_super_admin = true WHERE id = auth.uid()`, por el ORM o por PostgREST, y satisfacer con ello `current_user_is_super_admin()` — es decir, **114 predicados de policy** de este esquema y la frontera entera de RR-CAP-10 | **BLOCKER** | **CERRADO 2026-08-04 (tarde)** por `db/prepared/stella_0012_super_admin_column_acl.sql` (+ rollback). Hallado por la reauditoría final del diff, no por los dos revisores adversariales de la unidad anterior: **sus listas de ataque partían de `organizations` y de `stripe_webhook_events`, nunca de `users`** — la ausencia de una pregunta leída como ausencia de riesgo, que es la causa raíz que RR-CAP-12b documenta. **Por qué la cota de fila falla aquí y funciona en `organization_members`:** «puedes actualizar tu propia fila» es la regla correcta para un perfil y catastrófica en cuanto un PRIVILEGIO vive en esa fila; en `organization_members` la columna de autoridad (`role`) está acotada por un predicado de ROL (`members_update_admin`), no por propiedad, y por eso un `viewer` nunca pudo escalar por ahí. La propiedad es una cota segura para datos sobre ti; nunca para autoridad sobre ti. **Reparación:** `REVOKE UPDATE` de tabla completa a `authenticated`, `uellix_writer`, `anon` y `PUBLIC` sobre **ambas** tablas; `GRANT UPDATE (avatar_url, email, full_name, updated_at)` sobre `users` y `GRANT UPDATE (status, updated_at)` sobre `organization_members`, sólo a `uellix_writer`. `authenticated` no recibe nada: no tiene ningún *call site*. **Las cuatro columnas de `users` no son negociables y por poco se omiten:** el único write de la aplicación es `INSERT … ON CONFLICT (id) DO UPDATE SET email, full_name, avatar_url, updated_at` (`lib/auth/session.ts:304`), y `ON CONFLICT DO UPDATE` **exige el privilegio UPDATE sobre cada columna de su `SET`** — un `REVOKE` a secas habría roto el bootstrap de sesión en el **segundo** inicio de sesión de cada usuario, no en el primero. `INSERT` no se revoca (alta, membresía fundadora, invitación aceptada) y `handle_new_user()` fija `is_super_admin` a `false` en el camino que posee. **Evidencia:** `RR15-1..RR15-13` vivas y mutaciones `S-01..S-08` |
| **RR-CAP-03-B** | **La contención convertía una entrega de Stripe en una pérdida silenciosa.** `stripe_begin_event` fija `SET LOCAL lock_timeout = '3s'`, así que dos entregas concurrentes del mismo `event_id` podían terminar con el perdedor levantando `55P03` — colapsado por `WHEN OTHERS` en el `U0001` uniforme, indistinguible de un evento malformado. Si el manejador responde 4xx, Stripe deja de reintentar y el cambio de suscripción nunca aterriza: exactamente el fallo que CAP-03 existe para impedir | **MAJOR** | **CERRADO 2026-08-04 (tarde).** Tres manejadores **enumerados** —`lock_not_available`, `serialization_failure`, `deadlock_detected`— devuelven `'in_progress'`, que es la respuesta que la máquina de estados ya tenía para «otro está en ello» y que el manejador traduce a 5xx reintentable. Los tres son transitorios por definición y dejan la fila intacta, porque la subtransacción se deshizo. **`WHEN OTHERS` sigue fallando**, y eso es la mitad simétrica: responder «reintenta» a todo convertiría un defecto permanente en un bucle infinito de reintentos — la misma pérdida con la máscara contraria. **`57014` queda deliberadamente fuera**: lo levanta `statement_timeout`, que `uellix_stripe` lleva como ajuste de rol, y capturarlo para continuar arriesga que vuelva a dispararse en la sentencia siguiente. **Evidencia:** `CAP03-LOCK` (contendida, dos sesiones reales) y mutaciones `S-09..S-11` |
| **RR-CAP-8** | La exclusión de línea base por prefijo (`cap_`, `disclosures_`) es el mecanismo que hace independientes a los cinco paquetes **y** un punto ciego: cualquier policy creada con esos prefijos, sobre cualquier tabla y para cualquier rol, es invisible a las diez precondiciones y postcondiciones | NIT | ABIERTO — acotar la exclusión a nombres exactos es posible; no se hace para no acoplar las precondiciones al inventario de cada paquete |

**RR-CAP-0 actualizado — y ahora la FILA lo dice también.** Durante una ronda,
la tabla de arriba siguió afirmando «el diseño no se ha ejecutado contra ninguna
base» mientras este párrafo afirmaba lo contrario, y los ficheros que ambos
gobiernan citaban mediciones del ensayo. Un registro de riesgos que se
contradice a sí mismo no es un registro: es dos, y el lector elige. Corregido en
la fila, no sólo aquí. Los
cinco paquetes se aplicaron dos veces, se probaron con **72** aserciones vivas
y **seis** pruebas de concurrencia con sesiones reales, se revirtieron y se
reaplicaron — en un contenedor desechable **sin red**, sembrado desde un volcado
*schema-only* del stack local obtenido por lectura. Lo que sigue abierto es
distinto y más pequeño: **el dry run no es Supabase gestionado**. RR-CAP-5 y
RR-09 siguen intactos, y `uellix_stripe` (rol `LOGIN` nuevo) y los dos
`GRANT USAGE ON SCHEMA auth` son exactamente las tres cosas que Supabase
gestionado puede rechazar. **No verificado: el acceso remoto está prohibido en
esta unidad.**

{
  "manifest_id": "AV-PLATFORM-ADMIN-MFA-SUPPORT-DELETION",
  "node_or_batch": "batch",
  "authority_reference": "docs/ops/security/PLATFORM_ADMIN_MFA_SUPPORT_DELETION_AUTHORITY_v1.0.0.json (whole artifact; census at docs/ops/security/PLATFORM_ADMIN_RLS_SUPERADMIN_POLICY_CENSUS_v1.0.0.json). The issuer controls AV-P-07 and AV-N-16..AV-N-22, and the mutation controls AV-M-10/AV-M-11, are scoped under RAT-OP-01 (RATIFIED_BY_PRODUCT_OWNER), materialized at PR146 / commit 1cddd3109762f84710fdb81ad72820a87066a24f in docs/ops/commercial/COMMERCIAL_V1_MICRO_RATIFICATIONS_v1.0.0.json, and consumed by this authority's ratification_consumption block as SC-05A..SC-05G. PR146 was measured UNMERGED at R3; the ratification binds regardless, but final integration of PR145 waits on that provenance.",
  "dependencies": [
    "LANE-AU-TENANCY-S4-S5: status UNKNOWN — measured as an unmerged draft lane at base 88d95c65; pnpm ops:program-state could not be run in this worktree (no node_modules). Fails closed: no control below may assume AU is CLOSED.",
    "MO-07: status UNKNOWN — this manifest's own controls are a precondition of any MO-07 closure claim, per authority clause AD-04."
  ],
  "protected_surfaces": [],
  "protected_authority": null,
  "postgres_requirements": {
    "setup_manifest": "UNKNOWN",
    "probe_manifest": "UNKNOWN",
    "rationale": "The majority of these controls assert ROW-LEVEL SECURITY behaviour (disjunct removal, capability-gated content reads, cross-tenant refusal, NULL-organization split). RLS is only decidable against a real PostgreSQL instance; it cannot be proven by unit tests against a mock. The setup and probe manifests do not exist yet because this authority is DOCS_ONLY and authors no implementation — recorded as UNKNOWN so a validator fails closed rather than treating the requirement as satisfied. They MUST be authored by the successor implementation and MUST run via pnpm db:audit:disposable, never against staging or production."
  },
  "sentinels": [
    {
      "id": "SENTINEL-TENANT-B-CANARY",
      "purpose": "A canary row planted in tenant B's TENANT_CONTENT. It must NEVER appear in any result set read by a platform principal acting without a live support capability scoped to tenant B. Its appearance is direct proof of standing tenant-content access."
    },
    {
      "id": "SENTINEL-PURPOSE-STRING",
      "purpose": "A unique purpose string recorded at capability issue. It must appear verbatim in the per-use audit record of every access performed under that capability, proving purpose is bound to use and not merely to issuance."
    },
    {
      "id": "SENTINEL-REVOKED-CAPABILITY-ID",
      "purpose": "The identifier of a revoked capability. It must remain present and distinguishable in the audit record after revocation, proving revocation is durable and non-destructive (SC-18) rather than implemented as a delete."
    }
  ],
  "positive_controls": [
    {
      "id": "AV-P-01",
      "description": "STANDING-CONTENT-DENIAL, positive face: a platform principal holding a live, in-scope, unexpired, unrevoked support capability for organization A, with fresh AAL2, CAN read the specific TENANT_CONTENT rows of organization A that the capability's scope names.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-P-02",
      "description": "PURPOSE/SCOPE binding: issuing a capability records purpose, issued_by, issued_to, issued_at and expires_at; SENTINEL-PURPOSE-STRING appears verbatim in the per-use audit record of each access made under it, proving audit occurs at USE and carries the bound purpose (SC-07, SC-24).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-P-03",
      "description": "DESTRUCTIVE SoD happy path: user X requests a project deletion with a reason; a DISTINCT human user Y, with independently fresh AAL2, types the exact confirmation token and supplies their own reason; the deletion completes and BOTH the request and the approval produce durable audit records naming their own actor, reason, assurance evidence and time (SD-01..SD-06).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-P-04",
      "description": "COMMERCIAL METADATA BOUNDARY, positive face: a platform principal without any support capability CAN read the enumerated commercial/operational metadata of CM-03 and CM-04 (organization existence, status, membership edges, audit ledger) and each such read is audited with actor, operation, target and assurance evidence (CM-06).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-P-05",
      "description": "GLOBAL_REFERENCE split, positive face: after the SPLIT_THEN_REPLACE disposition, a platform principal CAN operate on rows of fx_rates, proxy_sources, financial_proxies and financial_proxy_versions where organization_id IS NULL, and each of those four tables' policies expresses the NULL and NOT NULL cases SEPARATELY rather than in one welded predicate (RS-02 GLOBAL_REFERENCE, CM-05).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-P-07",
      "description": "RAT-OP-01 AUTHORIZER, positive face: once every prerequisite is satisfied — including the SC-05F closure of the standing self-assignment path — a NATURAL PERSON holding an ACTIVE membership in the affected organization with role EXACTLY 'organization_admin' CAN authorize issuance of a support capability for that organization, and the issued capability records that person as issued_by. This control is expected to be UNRUNNABLE until SC-05F is closed; while the precondition is open it must be represented as such rather than reported green (see AV-N-19).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-P-06",
      "description": "CENSUS FALSIFIABILITY: re-running the deterministic policy census at the successor's tree reports a superadmin disjunct count strictly lower than the frozen baseline of 140, broken down per lineage (MIGRATION 127 / HOSTED_BASELINE_ONLY 4 / PREPARED_PACKET_ONLY 9) and per category, so that removal is observable rather than asserted (RS-03).",
      "test_file": "UNKNOWN"
    }
  ],
  "negative_controls": [
    {
      "id": "AV-N-01",
      "description": "STANDING-CONTENT-DENIAL: a platform principal with NO support capability reads TENANT_CONTENT and is REFUSED. SENTINEL-TENANT-B-CANARY must not appear in any result set. This must hold on ORDINARY tenant query paths, not only on /admin surfaces (RS-07), and must be asserted for at least one table from each of the 39 TENANT_CONTENT tables' distinct policy shapes.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-02",
      "description": "SUPPORT EXPIRY: a capability whose expires_at has passed grants NOTHING. Asserted at expires_at + 1 unit, and separately asserted that a capability issued with expires_at more than 60 minutes after issued_at is REFUSED AT ISSUE for normal access, and more than 240 minutes for escalated access (SC-09, SC-10).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-03",
      "description": "SUPPORT REVOCATION: a capability revoked by the issuing organization grants NOTHING for every evaluation after the revoking act commits, the revocation succeeds without platform cooperation and cannot be refused or delayed by the platform, and SENTINEL-REVOKED-CAPABILITY-ID remains present and distinguishable in the audit record afterwards (SC-15..SC-18).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-04",
      "description": "PURPOSE/SCOPE refusal: a capability scoped to entity set E grants nothing for a row outside E; a capability with a NULL, wildcard, list or 'all tenants' organization scope is REFUSED AT ISSUE, not merely at use (SC-01); and an unset or unparseable scope resolves to ZERO access, never to global access (SC-04).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-05",
      "description": "NO AUTO-RENEW: activity, session refresh, token refresh and a subsequent request each fail to extend expires_at. After expiry, access requires a wholly new capability with a new authorizing act; back-to-back issuance appears as SEPARATE capabilities in the audit record, each independently bounded from its OWN issued_at, never as one continuous access (SC-12..SC-14).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-06",
      "description": "AAL STALE: a platform principal whose AAL2 challenge was satisfied outside the freshness window is REFUSED, even though the session is valid and once reached AAL2. Asserted separately for capability use, for capability issue, and for each destructive-deletion actor (MA-02, MA-05, SD-03).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-07",
      "description": "AAL ABSENT: a platform principal who has never satisfied an AAL2 challenge is REFUSED, and the refusal is DISTINGUISHABLE from the AAL-stale refusal of AV-N-06 in both handling and audit. Additionally, where assurance state cannot be determined at all, the operation REFUSES rather than proceeding (MA-05, MA-06).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-08",
      "description": "SAME-PERSON REQUESTER/APPROVER: the person who requested a deletion CANNOT approve it. Asserted by natural-person identity, and asserted to still refuse when the two acts arrive in different sessions, different requests and different transactions — two sessions of one person are ONE actor (SD-01). Must also refuse when that person holds the tenant role 'super_admin' (SD-07).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-09",
      "description": "PLATFORM-VS-TENANT ROLE CONFUSION: holding the TENANT membership role 'super_admin' grants NO platform-principal capability whatsoever; and holding PLATFORM_PRINCIPAL grants NO tenant membership, no tenant role, and no position in any tenant role hierarchy. Asserted against a rank/hierarchy comparison helper as well as against the literal role string, because the documented bypass travels with the relation and with hasRole() rather than with the literal 'super_admin' appearing in a diff (PM-CONFLATE-01, PM-02).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-10",
      "description": "CROSS-TENANT SUPPORT CAPABILITY: a capability scoped to organization A grants NOTHING in organization B. Asserted to FAIL CLOSED — the refusal must not degrade to a broader scope, and must not be satisfiable by a null/unset current-organization context resolving to GLOBAL (SC-27, SC-04).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-11",
      "description": "PROHIBITED OPERATIONS refuse: under a live support capability, DELETE of tenant content is refused (SC-25); bulk export/enumeration without a bulk-scoped authorizing act is refused (SC-26); self-modification of the capability's own scope, purpose, expiry or revocation state is refused, as is issuing another capability (SC-28); modification of audit records, membership, roles or the platform principal set is refused (SC-29); and the capability cannot satisfy either destructive-deletion SoD actor (SC-30, SD-08).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-12",
      "description": "SELF-ISSUANCE refusal: a platform principal cannot issue a support capability to itself, cannot perform the authorizing act, and cannot write membership or roles to manufacture a tenant principal for itself (SC-05, CM-04 prohibited). Asserted including the case where the platform principal also legitimately holds tenant membership elsewhere.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-13",
      "description": "AUDIT LEDGER IMMUTABILITY: UPDATE and DELETE of audit_logs and domain_object_versions are refused for every principal including PLATFORM_PRINCIPAL — the audit record is not editable by the party it audits (CM-03 prohibited).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-14",
      "description": "NON-HUMAN ACTOR refusal: a service account, automation, scheduled job or machine identity is refused as either destructive-deletion SoD actor, and refused as issued_by or issued_to on a support capability (SD-02, SC fields issued_by/issued_to).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-15",
      "description": "APPROVER'S OWN REASON (SD-04): the approval is REFUSED server-side when the approval reason is absent, empty, or whitespace-only — asserted at the service boundary, not only in the UI. Separately, the approval is REFUSED when its reason is DEFAULTED OR DERIVED from the requester's reason, since SD-04 forbids that explicitly. The second assertion is the load-bearing one: at base 88d95c65 the sole caller supplies a non-empty synthesized template literal, so an emptiness check alone passes vacuously.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-16",
      "description": "NON-QUALIFYING TENANT ROLES DENIED (SC-05C, RAT-OP-01): a natural person with an ACTIVE membership in the affected organization is REFUSED as authorizer when their role is anything other than exactly 'organization_admin'. Asserted individually and by name for every other member of the closed role set in lib/auth/roles.ts: 'impact_manager' DENIED, 'analyst' DENIED, 'viewer' DENIED, and 'reviewer' DENIED. 'reviewer' is included for closed-world completeness even though it sits outside ROLE_HIERARCHY — a role absent from the hierarchy map must not be admitted by accident either.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-17",
      "description": "TENANT super_admin DENIED AS AUTHORIZER (SC-05D, RAT-OP-01): a natural person with an ACTIVE membership whose role is exactly 'super_admin' is REFUSED as authorizer. It must be refused DESPITE outranking organization_admin — measured: ROLE_HIERARCHY in lib/auth/roles.ts sets super_admin=100 and organization_admin=80, so hasRole('super_admin','organization_admin') at lib/auth/permissions.ts:25 returns TRUE. This control exists precisely to prove the implementation is NOT that comparison. Tenant super_admin is excluded deliberately and this refusal must never be relaxed as a convenience.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-18",
      "description": "PLATFORM PRINCIPAL DENIED AS AUTHORIZER, EVEN PRESENTING organization_admin (SC-05E, RAT-OP-01): a PLATFORM_PRINCIPAL is REFUSED as issuer even when it presents a genuine ACTIVE organization_admin membership in the affected organization — whether that membership was pre-existing, granted to it, or self-assigned. The platform standing must be evaluated independently of and in addition to the tenant-authorizer test, so possessing the qualifying role never launders the platform identity into a valid issuer.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-19",
      "description": "OPERATIONAL PRECONDITION FAILS CLOSED (SC-05F, RAT-OP-01): while the standing platform-admin self-assignment path remains OPEN, ANY support-capability issuance is REFUSED — including an issuance whose authorizer fully satisfies SC-05A..SC-05E. The refusal must be attributable to the unmet precondition, must not default-permit, and must not be silently skipped. Openness is decided by measurement, not configuration: at base 88d95c65 the path is open because db/migrations/0031_rls_core.sql policy members_insert_admin carries 'OR current_user_is_super_admin()' in its WITH CHECK.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-20",
      "description": "NO SELF-AUTHORIZATION (SC-05E, SC-28): a capability cannot authorize itself and cannot be used to authorize another. issued_by MUST NOT equal issued_to, asserted by natural-person identity — so a single subject who is simultaneously a platform principal and an ACTIVE organization_admin cannot occupy both ends of one issuance. Also asserted: an existing live capability cannot be presented as the authorizing act for a new one.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-21",
      "description": "CROSS-ORG AUTHORIZER DENIED (SC-05B, SC-01): a natural person holding an ACTIVE membership with role exactly 'organization_admin' in organization A is REFUSED as authorizer for a capability scoped to organization B. The membership must be in the AFFECTED organization. Asserted to fail closed, not to degrade to a broader scope.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-N-22",
      "description": "INACTIVE MEMBERSHIP DENIED (SC-05B): a natural person whose organization_admin membership in the affected organization is not ACTIVE — pending, suspended, revoked or removed — is REFUSED as authorizer. Role equality alone is insufficient; the membership state is a separate conjunct and must be evaluated at the moment of issue.",
      "test_file": "UNKNOWN"
    }
  ],
  "mutation_controls": [
    {
      "id": "AV-M-01",
      "description": "POLICY-LEVEL SUPERADMIN DISJUNCT REMOVAL/REPLACEMENT: re-introducing a current_user_is_super_admin() disjunct into ANY policy on a TENANT_CONTENT table must turn the suite RED via AV-N-01. The control must be proven non-vacuous by actually re-adding the disjunct to at least one such policy and observing the failure — a census count that merely drops is not sufficient evidence that the guard bites.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-02",
      "description": "SILENT SUBSTITUTION: replacing current_user_is_super_admin() with a differently-named helper whose truth conditions are IDENTICAL (i.e. reducing to a persistent boolean on the users row) must turn the suite RED. This is the mutation that a name-based or grep-based check cannot catch, and it is the specific failure mode RS-04 exists to prevent.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-03",
      "description": "QUERY-LEVEL ADMIN BYPASS: deleting the capability predicate from an ORDINARY (non-/admin) tenant query path, while leaving every /admin surface correctly gated, must turn the suite RED. This mutation is specifically designed to fail any suite that tests only admin surfaces — measured fact: all app/admin/*/actions.ts already call requireAdminAccess() independently, so an admin-only suite passes while the ordinary application still leaks.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-04",
      "description": "SoD COMPARISON REMOVAL: deleting the requester != approver comparison from the deletion approval path must turn the suite RED via AV-N-08. Non-vacuity is directly checkable at the frozen base, where the comparison is ABSENT: the control must be RED at base 88d95c65 and GREEN only after the fix, proving it measures the defect rather than the fix's spelling.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-05",
      "description": "EXPIRY WELD: changing the expiry comparison from absolute wall-clock against issued_at to an idle/activity-based timeout must turn the suite RED via AV-N-05, proving no-auto-renew is enforced structurally and not merely by the initial value of expires_at.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-06",
      "description": "NULL-ORGANIZATION WELD: replacing a split GLOBAL_REFERENCE policy with the single welded predicate (organization_id IS NULL OR organization_id = <current org>) must turn the suite RED via AV-N-10, because that shape opens a permanent cross-tenant channel whenever the current-organization value is itself null (CM-05 binding hazard).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-07",
      "description": "CLOSED-WORLD UNDER-CLOSURE: closing only the 127 MIGRATION-lineage disjuncts while leaving the 4 HOSTED_BASELINE_ONLY and 9 PREPARED_PACKET_ONLY disjuncts intact must NOT be reportable as closure — the census control AV-P-06 must fail on the residual lineages. This mutation encodes the exact under-closure that adopting F-AU-3's '~100' figure as the target would produce (AD-05).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-08",
      "description": "REVOCATION-AS-DELETE: implementing revocation as a row delete rather than a durable state change must turn the suite RED via AV-N-03's SENTINEL-REVOKED-CAPABILITY-ID assertion, proving a revoked capability stays distinguishable from one that never existed (SC-18).",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-09",
      "description": "SYNTHESIZED-REASON VACUITY (SD-04): replacing the approver-supplied reason input with a value derived from the requester's reason — the exact shape present at base 88d95c65, a non-empty template literal — must turn the suite RED via AV-N-15. This mutation exists because an emptiness-only check CANNOT catch it: the synthesized string always passes. It also guards the audit-integrity consequence, since that string is what lands in audit_logs.reason and afterJson.deleteReason and would otherwise credit the requester's justification to the approval.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-10",
      "description": "EXACT-EQUALITY REPLACED BY HIERARCHY/DOMINANCE (SC-05C/SC-05D, RAT-OP-01): replacing the exact-role equality test with a hierarchy or numeric-dominance comparison — hasRole(role,'organization_admin'), ROLE_HIERARCHY[role] >= ROLE_HIERARCHY['organization_admin'], a '>=' threshold, a '+' notation, or set membership such as role IN ('super_admin','organization_admin') — MUST turn the suite RED, and specifically the TENANT super_admin ADMISSION assertion AV-N-17 must be the control that goes RED. That directionality is the point of this mutation: the substitution is invisible to a suite that only tests the positive organization_admin case, because organization_admin satisfies BOTH the equality and the threshold. Only the super_admin case distinguishes them — measured: super_admin=100 >= organization_admin=80, so every dominance form silently admits the role RAT-OP-01 excludes.",
      "test_file": "UNKNOWN"
    },
    {
      "id": "AV-M-11",
      "description": "PRECONDITION BYPASS (SC-05F): deleting or short-circuiting the SC-05F fail-closed precondition check — so that issuance proceeds on a satisfied authorizer test while the standing self-assignment path is still open — MUST turn the suite RED via AV-N-19. This guards the specific failure in which a correct-looking SC-05A..SC-05E implementation ships and is reported as satisfying RAT-OP-01, while remaining circumventable by a single INSERT into organization_members.",
      "test_file": "UNKNOWN"
    }
  ],
  "exit_gates": [
    "typecheck",
    "secrets_scan",
    "authority_seal_verify",
    "ods_scope",
    "ods_poststate",
    "targeted_tests",
    "postgres_disposable_probe"
  ]
}

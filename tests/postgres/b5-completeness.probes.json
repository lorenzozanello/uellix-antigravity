{
 "probes": [
  {
   "id": "PG-POS-1-same-tenant-SELECT",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a100000-0000-4000-8000-0000000000a1\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.readiness_assessments WHERE id='6a100000-0000-4000-8000-0000000006a1')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'PG-POS-1 readiness_assessments measured=% expected=1', v; END IF;\n  SELECT (SELECT count(*) FROM public.sensitivity_candidates WHERE id='7a100000-0000-4000-8000-0000000007a1')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'PG-POS-1 sensitivity_candidates measured=% expected=1', v; END IF;\n  SELECT (SELECT count(*) FROM public.sensitivity_scenarios WHERE id='8a100000-0000-4000-8000-0000000008a1')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'PG-POS-1 sensitivity_scenarios measured=% expected=1', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "PG-POS-1-same-tenant-INSERT",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a100000-0000-4000-8000-0000000000a1\",\"role\":\"authenticated\"}', true);\nDO $i$ DECLARE n int; new_sc uuid; BEGIN\n  INSERT INTO public.readiness_assessments (organization_id, project_id, calculation_run_id, readiness_model_version, global_score, band, dimension_scores, criteria_detail, created_by)\n    VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','1.0.0','60.00','partial_preparation','{}','[]','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT; IF n <> 1 THEN RAISE EXCEPTION 'PG-POS-1 readiness_assessments INSERT ROW_COUNT=% expected=1', n; END IF;\n\n  INSERT INTO public.sensitivity_candidates (organization_id, project_id, calculation_run_id, candidate_key, candidate_kind, input_reference, base_value, disposition, sensitivity_model_version, created_by)\n    VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','pos1:candidate','methodological_filter','{}','0','pending','1.0.0','0a100000-0000-4000-8000-0000000000a1') RETURNING id INTO new_sc;\n  GET DIAGNOSTICS n = ROW_COUNT; IF n <> 1 THEN RAISE EXCEPTION 'PG-POS-1 sensitivity_candidates INSERT ROW_COUNT=% expected=1', n; END IF;\n\n  INSERT INTO public.sensitivity_scenarios (organization_id, project_id, calculation_run_id, scenario_kind, candidate_ids, modified_inputs, reason, sensitivity_model_version, calculation_engine_version, result_json, base_result_json, selected_by, created_by)\n    VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','one_at_a_time','[]','[]','pos1 reason','1.0.0','1.0.0','{}','{}','0a100000-0000-4000-8000-0000000000a1','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT; IF n <> 1 THEN RAISE EXCEPTION 'PG-POS-1 sensitivity_scenarios INSERT ROW_COUNT=% expected=1', n; END IF;\nEND $i$;\nROLLBACK;\n"
  },
  {
   "id": "PG-POS-2-CHECK-range-global_score",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'readiness_assessments_global_score_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 global_score: constraint readiness_assessments_global_score_check does not exist'; END IF;\n  IF def NOT LIKE '%>= 0%' THEN RAISE EXCEPTION 'PG-POS-2 global_score missing lower bound in %', def; END IF;\n  IF def NOT LIKE '%<= 100%' THEN RAISE EXCEPTION 'PG-POS-2 global_score missing upper bound in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-band",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'readiness_assessments_band_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 band: constraint readiness_assessments_band_check does not exist'; END IF;\n  IF def NOT LIKE '%initial_preparation%' THEN RAISE EXCEPTION 'PG-POS-2 band missing value initial_preparation in %', def; END IF;\n  IF def NOT LIKE '%partial_preparation%' THEN RAISE EXCEPTION 'PG-POS-2 band missing value partial_preparation in %', def; END IF;\n  IF def NOT LIKE '%advanced_preparation%' THEN RAISE EXCEPTION 'PG-POS-2 band missing value advanced_preparation in %', def; END IF;\n  IF def NOT LIKE '%high_preparation%' THEN RAISE EXCEPTION 'PG-POS-2 band missing value high_preparation in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-candidate_kind",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'sensitivity_candidates_candidate_kind_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 candidate_kind: constraint sensitivity_candidates_candidate_kind_check does not exist'; END IF;\n  IF def NOT LIKE '%methodological_filter%' THEN RAISE EXCEPTION 'PG-POS-2 candidate_kind missing value methodological_filter in %', def; END IF;\n  IF def NOT LIKE '%structured_assumption%' THEN RAISE EXCEPTION 'PG-POS-2 candidate_kind missing value structured_assumption in %', def; END IF;\n  IF def NOT LIKE '%proxy_value%' THEN RAISE EXCEPTION 'PG-POS-2 candidate_kind missing value proxy_value in %', def; END IF;\n  IF def NOT LIKE '%other_quantitative_input%' THEN RAISE EXCEPTION 'PG-POS-2 candidate_kind missing value other_quantitative_input in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-disposition",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'sensitivity_candidates_disposition_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 disposition: constraint sensitivity_candidates_disposition_check does not exist'; END IF;\n  IF def NOT LIKE '%variation_required%' THEN RAISE EXCEPTION 'PG-POS-2 disposition missing value variation_required in %', def; END IF;\n  IF def NOT LIKE '%no_additional_variation_required%' THEN RAISE EXCEPTION 'PG-POS-2 disposition missing value no_additional_variation_required in %', def; END IF;\n  IF def NOT LIKE '%pending%' THEN RAISE EXCEPTION 'PG-POS-2 disposition missing value pending in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-scenario_kind",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'sensitivity_scenarios_scenario_kind_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 scenario_kind: constraint sensitivity_scenarios_scenario_kind_check does not exist'; END IF;\n  IF def NOT LIKE '%one_at_a_time%' THEN RAISE EXCEPTION 'PG-POS-2 scenario_kind missing value one_at_a_time in %', def; END IF;\n  IF def NOT LIKE '%combined%' THEN RAISE EXCEPTION 'PG-POS-2 scenario_kind missing value combined in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-3-same-tenant-UPDATE-sensitivity_candidates",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a100000-0000-4000-8000-0000000000a1\",\"role\":\"authenticated\"}', true);\nDO $u$ DECLARE n int; BEGIN\n  UPDATE public.sensitivity_candidates SET disposition = 'variation_required', rationale = 'pos3 rationale', dispositioned_by = '0a100000-0000-4000-8000-0000000000a1', dispositioned_at = now() WHERE id = '7a100000-0000-4000-8000-0000000007a1';\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'PG-POS-3 sensitivity_candidates same-tenant UPDATE ROW_COUNT=% expected=1', n; END IF;\nEND $u$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N1-cross-tenant-SELECT-readiness_assessments",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b100000-0000-4000-8000-0000000000b1\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.readiness_assessments WHERE id='6a100000-0000-4000-8000-0000000006a1')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'SEC-N1 readiness_assessments cross-tenant SELECT measured=% expected=0', v; END IF;\n  SELECT (SELECT count(*) FROM public.readiness_assessments WHERE id='6b100000-0000-4000-8000-0000000006b1')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'SEC-N1 readiness_assessments own-tenant SELECT measured=% expected=1 (control that the query itself is not vacuously empty)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N1-cross-tenant-SELECT-sensitivity_candidates",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b100000-0000-4000-8000-0000000000b1\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.sensitivity_candidates WHERE id='7a100000-0000-4000-8000-0000000007a1')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'SEC-N1 sensitivity_candidates cross-tenant SELECT measured=% expected=0', v; END IF;\n  SELECT (SELECT count(*) FROM public.sensitivity_candidates WHERE id='7b100000-0000-4000-8000-0000000007b1')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'SEC-N1 sensitivity_candidates own-tenant SELECT measured=% expected=1 (control that the query itself is not vacuously empty)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N1-cross-tenant-SELECT-sensitivity_scenarios",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b100000-0000-4000-8000-0000000000b1\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.sensitivity_scenarios WHERE id='8a100000-0000-4000-8000-0000000008a1')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'SEC-N1 sensitivity_scenarios cross-tenant SELECT measured=% expected=0', v; END IF;\n  SELECT (SELECT count(*) FROM public.sensitivity_scenarios WHERE id='8b100000-0000-4000-8000-0000000008b1')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'SEC-N1 sensitivity_scenarios own-tenant SELECT measured=% expected=1 (control that the query itself is not vacuously empty)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N2-cross-tenant-INSERT-readiness_assessments",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b100000-0000-4000-8000-0000000000b1\",\"role\":\"authenticated\"}', true);\nDO $e$ DECLARE caught text := 'none'; BEGIN\n  BEGIN\n    INSERT INTO public.readiness_assessments (organization_id, project_id, calculation_run_id, readiness_model_version, global_score, band, dimension_scores, criteria_detail, created_by)\n      VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','1.0.0','60.00','partial_preparation','{}','[]','0b100000-0000-4000-8000-0000000000b1');\n  EXCEPTION WHEN OTHERS THEN caught := SQLSTATE; END;\n  IF caught <> '42501' THEN RAISE EXCEPTION 'SEC-N2 readiness_assessments caught=% expected=42501', caught; END IF;\nEND $e$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N2-cross-tenant-INSERT-sensitivity_candidates",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b100000-0000-4000-8000-0000000000b1\",\"role\":\"authenticated\"}', true);\nDO $e$ DECLARE caught text := 'none'; BEGIN\n  BEGIN\n    INSERT INTO public.sensitivity_candidates (organization_id, project_id, calculation_run_id, candidate_key, candidate_kind, input_reference, base_value, disposition, sensitivity_model_version, created_by)\n      VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','secn2:candidate','methodological_filter','{}','0','pending','1.0.0','0b100000-0000-4000-8000-0000000000b1');\n  EXCEPTION WHEN OTHERS THEN caught := SQLSTATE; END;\n  IF caught <> '42501' THEN RAISE EXCEPTION 'SEC-N2 sensitivity_candidates caught=% expected=42501', caught; END IF;\nEND $e$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N2-cross-tenant-INSERT-sensitivity_scenarios",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b100000-0000-4000-8000-0000000000b1\",\"role\":\"authenticated\"}', true);\nDO $e$ DECLARE caught text := 'none'; BEGIN\n  BEGIN\n    INSERT INTO public.sensitivity_scenarios (organization_id, project_id, calculation_run_id, scenario_kind, candidate_ids, modified_inputs, reason, sensitivity_model_version, calculation_engine_version, result_json, base_result_json, selected_by, created_by)\n      VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','one_at_a_time','[]','[]','secn2 reason','1.0.0','1.0.0','{}','{}','0b100000-0000-4000-8000-0000000000b1','0b100000-0000-4000-8000-0000000000b1');\n  EXCEPTION WHEN OTHERS THEN caught := SQLSTATE; END;\n  IF caught <> '42501' THEN RAISE EXCEPTION 'SEC-N2 sensitivity_scenarios caught=% expected=42501', caught; END IF;\nEND $e$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N3-cross-tenant-UPDATE-sensitivity_candidates",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b100000-0000-4000-8000-0000000000b1\",\"role\":\"authenticated\"}', true);\nDO $u$ DECLARE n int; BEGIN\n  UPDATE public.sensitivity_candidates SET rationale = 'secn3' WHERE id = '7a100000-0000-4000-8000-0000000007a1';\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 0 THEN RAISE EXCEPTION 'SEC-N3 sensitivity_candidates ROW_COUNT=% expected=0', n; END IF;\nEND $u$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N3-readiness_assessments-has-no-UPDATE-policy-at-all",
   "sql": "DO $p$ DECLARE n int; BEGIN\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='readiness_assessments' AND cmd='UPDATE';\n  IF n <> 0 THEN RAISE EXCEPTION 'SEC-N3 readiness_assessments unexpectedly carries an UPDATE policy: %', n; END IF;\nEND $p$;\n"
  },
  {
   "id": "SEC-N3-sensitivity_scenarios-has-no-UPDATE-policy-at-all",
   "sql": "DO $p$ DECLARE n int; BEGIN\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='sensitivity_scenarios' AND cmd='UPDATE';\n  IF n <> 0 THEN RAISE EXCEPTION 'SEC-N3 sensitivity_scenarios unexpectedly carries an UPDATE policy: %', n; END IF;\nEND $p$;\n"
  },
  {
   "id": "SEC-N4-no-new-function-surface-VACUOUS-BY-ABSENCE",
   "sql": "DO $f$ DECLARE n int; BEGIN\n  SELECT count(*) INTO n FROM pg_proc p JOIN pg_namespace ns ON ns.oid = p.pronamespace\n  WHERE ns.nspname = 'public' AND p.proname IN (\n    'readiness_assessments', 'sensitivity_candidates', 'sensitivity_scenarios',\n    'readiness_assessment', 'sensitivity_candidate', 'sensitivity_scenario'\n  );\n  IF n <> 0 THEN RAISE EXCEPTION 'SEC-N4 expected zero B5-owned functions, measured=%', n; END IF;\nEND $f$;\n"
  },
  {
   "id": "SEC-ACL-1-RLS-enabled-and-policies-exist-per-table",
   "sql": "DO $s$ DECLARE n int; BEGIN\n  SELECT count(*) INTO n FROM pg_class c JOIN pg_namespace ns ON ns.oid = c.relnamespace\n  WHERE ns.nspname = 'public' AND c.relname IN ('readiness_assessments','sensitivity_candidates','sensitivity_scenarios') AND c.relrowsecurity = true;\n  IF n <> 3 THEN RAISE EXCEPTION 'SEC-ACL-1 relrowsecurity measured=% expected=3', n; END IF;\n\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='readiness_assessments';\n  IF n <> 2 THEN RAISE EXCEPTION 'SEC-ACL-1 readiness_assessments policy count measured=% expected=2', n; END IF;\n\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='sensitivity_candidates';\n  IF n <> 3 THEN RAISE EXCEPTION 'SEC-ACL-1 sensitivity_candidates policy count measured=% expected=3', n; END IF;\n\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='sensitivity_scenarios';\n  IF n <> 2 THEN RAISE EXCEPTION 'SEC-ACL-1 sensitivity_scenarios policy count measured=% expected=2', n; END IF;\nEND $s$;\n"
  },
  {
   "id": "MUT-PG-1-SELECT-policy-removal-caught-readiness_assessments",
   "sql": "BEGIN;\nDROP POLICY readiness_assessments_select ON public.readiness_assessments;\nSET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a100000-0000-4000-8000-0000000000a1\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.readiness_assessments WHERE id='6a100000-0000-4000-8000-0000000006a1')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'MUT-PG-1 readiness_assessments measured=% expected=0 (SELECT policy removal must be caught)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-1-SELECT-policy-removal-caught-sensitivity_candidates",
   "sql": "BEGIN;\nDROP POLICY sensitivity_candidates_select ON public.sensitivity_candidates;\nSET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a100000-0000-4000-8000-0000000000a1\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.sensitivity_candidates WHERE id='7a100000-0000-4000-8000-0000000007a1')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'MUT-PG-1 sensitivity_candidates measured=% expected=0 (SELECT policy removal must be caught)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-1-SELECT-policy-removal-caught-sensitivity_scenarios",
   "sql": "BEGIN;\nDROP POLICY sensitivity_scenarios_select ON public.sensitivity_scenarios;\nSET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a100000-0000-4000-8000-0000000000a1\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.sensitivity_scenarios WHERE id='8a100000-0000-4000-8000-0000000008a1')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'MUT-PG-1 sensitivity_scenarios measured=% expected=0 (SELECT policy removal must be caught)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-1-UPDATE-policy-removal-caught-sensitivity_candidates",
   "sql": "BEGIN;\nDROP POLICY sensitivity_candidates_update ON public.sensitivity_candidates;\nSET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a100000-0000-4000-8000-0000000000a1\",\"role\":\"authenticated\"}', true);\nDO $u$ DECLARE n int; BEGIN\n  UPDATE public.sensitivity_candidates SET rationale = 'mutpg1' WHERE id = '7a100000-0000-4000-8000-0000000007a1';\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 0 THEN RAISE EXCEPTION 'MUT-PG-1 sensitivity_candidates UPDATE measured=% expected=0 (UPDATE policy removal must be caught, even for the row''s own tenant)', n; END IF;\nEND $u$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-global_score",
   "sql": "BEGIN;\nALTER TABLE public.readiness_assessments DROP CONSTRAINT readiness_assessments_global_score_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.readiness_assessments (organization_id, project_id, calculation_run_id, readiness_model_version, global_score, band, dimension_scores, criteria_detail, created_by) VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','1.0.0','150.00','partial_preparation','{}','[]','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 global_score expected the out-of-range INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-band",
   "sql": "BEGIN;\nALTER TABLE public.readiness_assessments DROP CONSTRAINT readiness_assessments_band_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.readiness_assessments (organization_id, project_id, calculation_run_id, readiness_model_version, global_score, band, dimension_scores, criteria_detail, created_by) VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','1.0.0','60.00','invented_value','{}','[]','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 band expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-candidate_kind",
   "sql": "BEGIN;\nALTER TABLE public.sensitivity_candidates DROP CONSTRAINT sensitivity_candidates_candidate_kind_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.sensitivity_candidates (organization_id, project_id, calculation_run_id, candidate_key, candidate_kind, input_reference, base_value, disposition, sensitivity_model_version, created_by) VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','mut2:candidate_kind','invented_value','{}','0','pending','1.0.0','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 candidate_kind expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-disposition",
   "sql": "BEGIN;\nALTER TABLE public.sensitivity_candidates DROP CONSTRAINT sensitivity_candidates_disposition_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.sensitivity_candidates (organization_id, project_id, calculation_run_id, candidate_key, candidate_kind, input_reference, base_value, disposition, sensitivity_model_version, created_by) VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','mut2:disposition','methodological_filter','{}','0','invented_value','1.0.0','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 disposition expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-scenario_kind",
   "sql": "BEGIN;\nALTER TABLE public.sensitivity_scenarios DROP CONSTRAINT sensitivity_scenarios_scenario_kind_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.sensitivity_scenarios (organization_id, project_id, calculation_run_id, scenario_kind, candidate_ids, modified_inputs, reason, sensitivity_model_version, calculation_engine_version, result_json, base_result_json, selected_by, created_by) VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','invented_value','[]','[]','mut2','1.0.0','1.0.0','{}','{}','0a100000-0000-4000-8000-0000000000a1','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 scenario_kind expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-3-conditional-NOT-NULL-removal-caught-rationale",
   "sql": "BEGIN;\nALTER TABLE public.sensitivity_candidates DROP CONSTRAINT sensitivity_candidates_rationale_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.sensitivity_candidates (organization_id, project_id, calculation_run_id, candidate_key, candidate_kind, input_reference, base_value, disposition, rationale, sensitivity_model_version, created_by)\n    VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','mut3:rationale','methodological_filter','{}','0','variation_required',NULL,'1.0.0','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-3 rationale expected the missing-rationale INSERT to SUCCEED once the CHECK is dropped (n=%)', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-3-conditional-NOT-NULL-removal-caught-combination_description",
   "sql": "BEGIN;\nALTER TABLE public.sensitivity_scenarios DROP CONSTRAINT sensitivity_scenarios_combination_description_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.sensitivity_scenarios (organization_id, project_id, calculation_run_id, scenario_kind, candidate_ids, modified_inputs, reason, combination_description, sensitivity_model_version, calculation_engine_version, result_json, base_result_json, selected_by, created_by)\n    VALUES ('1a100000-0000-4000-8000-0000000001a1','2a100000-0000-4000-8000-0000000002a1','5a100000-0000-4000-8000-0000000005a2','combined','[]','[]','mut3',NULL,'1.0.0','1.0.0','{}','{}','0a100000-0000-4000-8000-0000000000a1','0a100000-0000-4000-8000-0000000000a1');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-3 combination_description expected the missing-description combined-scenario INSERT to SUCCEED once the CHECK is dropped (n=%)', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-4-EXECUTE-revocation-VACUOUS-BY-ABSENCE",
   "sql": "DO $f$ BEGIN\n  -- B5 introduces no SECURITY DEFINER function and no REVOKE EXECUTE\n  -- statement (see SEC-N4 above) — there is nothing to remove and no\n  -- non-vacuous mutation this control can perform. Recorded explicitly\n  -- as required, never as a silently-omitted control.\n  RAISE NOTICE 'MUT-PG-4: VACUOUS-BY-ABSENCE — B5 introduces no function/SECURITY DEFINER surface (see SEC-N4)';\nEND $f$;\n"
  }
 ]
}

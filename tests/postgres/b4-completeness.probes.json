{
 "probes": [
  {
   "id": "PG-POS-1-same-tenant-SELECT",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a000000-0000-4000-8000-00000000000a\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.methodological_assumptions WHERE id='7a000000-0000-4000-8000-00000000007a')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'PG-POS-1 methodological_assumptions measured=% expected=1', v; END IF;\n  SELECT (SELECT count(*) FROM public.assumption_object_links WHERE id='8a000000-0000-4000-8000-00000000008a')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'PG-POS-1 assumption_object_links measured=% expected=1', v; END IF;\n  SELECT (SELECT count(*) FROM public.counterfactual_assessments WHERE id='9a000000-0000-4000-8000-00000000009a')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'PG-POS-1 counterfactual_assessments measured=% expected=1', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "PG-POS-1-same-tenant-INSERT",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a000000-0000-4000-8000-00000000000a\",\"role\":\"authenticated\"}', true);\nDO $i$ DECLARE n int; new_ma uuid; BEGIN\n  INSERT INTO public.methodological_assumptions (organization_id, project_id, formulation, rationale, basis_type, materiality_flag, created_by)\n    VALUES ('1a000000-0000-4000-8000-00000000001a','2a000000-0000-4000-8000-00000000002a','pos1 formulation','pos1 rationale','derived','non_material','0a000000-0000-4000-8000-00000000000a') RETURNING id INTO new_ma;\n  GET DIAGNOSTICS n = ROW_COUNT; IF n <> 1 THEN RAISE EXCEPTION 'PG-POS-1 ma INSERT ROW_COUNT=% expected=1', n; END IF;\n\n  INSERT INTO public.assumption_object_links (organization_id, assumption_id, affected_object_type, affected_object_id, created_by)\n    VALUES ('1a000000-0000-4000-8000-00000000001a', new_ma, 'outcome', '4a000000-0000-4000-8000-00000000004a', '0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT; IF n <> 1 THEN RAISE EXCEPTION 'PG-POS-1 aol INSERT ROW_COUNT=% expected=1', n; END IF;\n\n  INSERT INTO public.counterfactual_assessments (organization_id, outcome_id, calculation_run_id, baseline_availability, basis_kind, deadweight_support_state, rationale, created_by)\n    VALUES ('1a000000-0000-4000-8000-00000000001a', '4a000000-0000-4000-8000-00000000004b', '5a000000-0000-4000-8000-00000000005a', 'not_applicable', 'documented_assumption', 'supported', 'pos1 ca rationale', '0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT; IF n <> 1 THEN RAISE EXCEPTION 'PG-POS-1 ca INSERT ROW_COUNT=% expected=1', n; END IF;\nEND $i$;\nROLLBACK;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-baseline_availability",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'counterfactual_assessments_baseline_availability_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 baseline_availability: constraint counterfactual_assessments_baseline_availability_check does not exist'; END IF;\n  IF def NOT LIKE '%available%' THEN RAISE EXCEPTION 'PG-POS-2 baseline_availability missing value available in %', def; END IF;\n  IF def NOT LIKE '%not_available%' THEN RAISE EXCEPTION 'PG-POS-2 baseline_availability missing value not_available in %', def; END IF;\n  IF def NOT LIKE '%not_applicable%' THEN RAISE EXCEPTION 'PG-POS-2 baseline_availability missing value not_applicable in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-basis_kind",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'counterfactual_assessments_basis_kind_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 basis_kind: constraint counterfactual_assessments_basis_kind_check does not exist'; END IF;\n  IF def NOT LIKE '%baseline_observation%' THEN RAISE EXCEPTION 'PG-POS-2 basis_kind missing value baseline_observation in %', def; END IF;\n  IF def NOT LIKE '%comparison_group%' THEN RAISE EXCEPTION 'PG-POS-2 basis_kind missing value comparison_group in %', def; END IF;\n  IF def NOT LIKE '%historical_trend%' THEN RAISE EXCEPTION 'PG-POS-2 basis_kind missing value historical_trend in %', def; END IF;\n  IF def NOT LIKE '%benchmark%' THEN RAISE EXCEPTION 'PG-POS-2 basis_kind missing value benchmark in %', def; END IF;\n  IF def NOT LIKE '%statistic%' THEN RAISE EXCEPTION 'PG-POS-2 basis_kind missing value statistic in %', def; END IF;\n  IF def NOT LIKE '%literature%' THEN RAISE EXCEPTION 'PG-POS-2 basis_kind missing value literature in %', def; END IF;\n  IF def NOT LIKE '%stakeholder_evidence%' THEN RAISE EXCEPTION 'PG-POS-2 basis_kind missing value stakeholder_evidence in %', def; END IF;\n  IF def NOT LIKE '%documented_assumption%' THEN RAISE EXCEPTION 'PG-POS-2 basis_kind missing value documented_assumption in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-deadweight_support_state",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'counterfactual_assessments_deadweight_support_state_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 deadweight_support_state: constraint counterfactual_assessments_deadweight_support_state_check does not exist'; END IF;\n  IF def NOT LIKE '%supported%' THEN RAISE EXCEPTION 'PG-POS-2 deadweight_support_state missing value supported in %', def; END IF;\n  IF def NOT LIKE '%unknown_or_insufficient%' THEN RAISE EXCEPTION 'PG-POS-2 deadweight_support_state missing value unknown_or_insufficient in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-basis_type",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'methodological_assumptions_basis_type_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 basis_type: constraint methodological_assumptions_basis_type_check does not exist'; END IF;\n  IF def NOT LIKE '%evidence_or_external_source%' THEN RAISE EXCEPTION 'PG-POS-2 basis_type missing value evidence_or_external_source in %', def; END IF;\n  IF def NOT LIKE '%derived%' THEN RAISE EXCEPTION 'PG-POS-2 basis_type missing value derived in %', def; END IF;\n  IF def NOT LIKE '%documented_human_judgement%' THEN RAISE EXCEPTION 'PG-POS-2 basis_type missing value documented_human_judgement in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "PG-POS-2-CHECK-vocabulary-materiality_flag",
   "sql": "DO $c$ DECLARE def text; BEGIN\n  SELECT pg_get_constraintdef(oid) INTO def FROM pg_constraint WHERE conname = 'methodological_assumptions_materiality_flag_check';\n  IF def IS NULL THEN RAISE EXCEPTION 'PG-POS-2 materiality_flag: constraint methodological_assumptions_materiality_flag_check does not exist'; END IF;\n  IF def NOT LIKE '%material%' THEN RAISE EXCEPTION 'PG-POS-2 materiality_flag missing value material in %', def; END IF;\n  IF def NOT LIKE '%non_material%' THEN RAISE EXCEPTION 'PG-POS-2 materiality_flag missing value non_material in %', def; END IF;\nEND $c$;\n"
  },
  {
   "id": "SEC-N1-cross-tenant-SELECT-methodological_assumptions",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b000000-0000-4000-8000-00000000000b\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.methodological_assumptions WHERE id='7a000000-0000-4000-8000-00000000007a')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'SEC-N1 methodological_assumptions cross-tenant SELECT measured=% expected=0', v; END IF;\n  SELECT (SELECT count(*) FROM public.methodological_assumptions WHERE id='7b000000-0000-4000-8000-00000000007b')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'SEC-N1 methodological_assumptions own-tenant SELECT measured=% expected=1 (control that the query itself is not vacuously empty)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N1-cross-tenant-SELECT-assumption_object_links",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b000000-0000-4000-8000-00000000000b\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.assumption_object_links WHERE id='8a000000-0000-4000-8000-00000000008a')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'SEC-N1 assumption_object_links cross-tenant SELECT measured=% expected=0', v; END IF;\n  SELECT (SELECT count(*) FROM public.assumption_object_links WHERE id='8b000000-0000-4000-8000-00000000008b')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'SEC-N1 assumption_object_links own-tenant SELECT measured=% expected=1 (control that the query itself is not vacuously empty)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N1-cross-tenant-SELECT-counterfactual_assessments",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b000000-0000-4000-8000-00000000000b\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.counterfactual_assessments WHERE id='9a000000-0000-4000-8000-00000000009a')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'SEC-N1 counterfactual_assessments cross-tenant SELECT measured=% expected=0', v; END IF;\n  SELECT (SELECT count(*) FROM public.counterfactual_assessments WHERE id='9b000000-0000-4000-8000-00000000009b')::text INTO v;\n  IF v IS DISTINCT FROM '1' THEN RAISE EXCEPTION 'SEC-N1 counterfactual_assessments own-tenant SELECT measured=% expected=1 (control that the query itself is not vacuously empty)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N2-cross-tenant-INSERT-methodological_assumptions",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b000000-0000-4000-8000-00000000000b\",\"role\":\"authenticated\"}', true);\nDO $e$ DECLARE caught text := 'none'; BEGIN\n  BEGIN\n    INSERT INTO public.methodological_assumptions (organization_id, project_id, formulation, rationale, basis_type, materiality_flag, created_by)\n      VALUES ('1a000000-0000-4000-8000-00000000001a','2a000000-0000-4000-8000-00000000002a','secn2','secn2','derived','non_material','0b000000-0000-4000-8000-00000000000b');\n  EXCEPTION WHEN OTHERS THEN caught := SQLSTATE; END;\n  IF caught <> '42501' THEN RAISE EXCEPTION 'SEC-N2 methodological_assumptions caught=% expected=42501', caught; END IF;\nEND $e$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N2-cross-tenant-INSERT-assumption_object_links",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b000000-0000-4000-8000-00000000000b\",\"role\":\"authenticated\"}', true);\nDO $e$ DECLARE caught text := 'none'; BEGIN\n  BEGIN\n    INSERT INTO public.assumption_object_links (organization_id, assumption_id, affected_object_type, affected_object_id, created_by)\n      VALUES ('1a000000-0000-4000-8000-00000000001a','7a000000-0000-4000-8000-00000000007a','outcome','4a000000-0000-4000-8000-00000000004a','0b000000-0000-4000-8000-00000000000b');\n  EXCEPTION WHEN OTHERS THEN caught := SQLSTATE; END;\n  IF caught <> '42501' THEN RAISE EXCEPTION 'SEC-N2 assumption_object_links caught=% expected=42501', caught; END IF;\nEND $e$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N2-cross-tenant-INSERT-counterfactual_assessments",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b000000-0000-4000-8000-00000000000b\",\"role\":\"authenticated\"}', true);\nDO $e$ DECLARE caught text := 'none'; BEGIN\n  BEGIN\n    INSERT INTO public.counterfactual_assessments (organization_id, outcome_id, calculation_run_id, baseline_availability, basis_kind, deadweight_support_state, rationale, created_by)\n      VALUES ('1a000000-0000-4000-8000-00000000001a','4a000000-0000-4000-8000-00000000004a','5a000000-0000-4000-8000-00000000005a','not_applicable','documented_assumption','supported','secn2','0b000000-0000-4000-8000-00000000000b');\n  EXCEPTION WHEN OTHERS THEN caught := SQLSTATE; END;\n  IF caught <> '42501' THEN RAISE EXCEPTION 'SEC-N2 counterfactual_assessments caught=% expected=42501', caught; END IF;\nEND $e$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N3-cross-tenant-UPDATE-methodological_assumptions",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b000000-0000-4000-8000-00000000000b\",\"role\":\"authenticated\"}', true);\nDO $u$ DECLARE n int; BEGIN\n  UPDATE public.methodological_assumptions SET rationale = 'secn3' WHERE id = '7a000000-0000-4000-8000-00000000007a';\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 0 THEN RAISE EXCEPTION 'SEC-N3 methodological_assumptions ROW_COUNT=% expected=0', n; END IF;\nEND $u$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N3-cross-tenant-UPDATE-counterfactual_assessments",
   "sql": "BEGIN; SET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0b000000-0000-4000-8000-00000000000b\",\"role\":\"authenticated\"}', true);\nDO $u$ DECLARE n int; BEGIN\n  UPDATE public.counterfactual_assessments SET rationale = 'secn3' WHERE id = '9a000000-0000-4000-8000-00000000009a';\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 0 THEN RAISE EXCEPTION 'SEC-N3 counterfactual_assessments ROW_COUNT=% expected=0', n; END IF;\nEND $u$;\nROLLBACK;\n"
  },
  {
   "id": "SEC-N3-assumption_object_links-has-no-UPDATE-policy-at-all",
   "sql": "DO $p$ DECLARE n int; BEGIN\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='assumption_object_links' AND cmd='UPDATE';\n  IF n <> 0 THEN RAISE EXCEPTION 'SEC-N3 assumption_object_links unexpectedly carries an UPDATE policy: %', n; END IF;\nEND $p$;\n"
  },
  {
   "id": "SEC-N4-no-new-function-surface-VACUOUS-BY-ABSENCE",
   "sql": "DO $f$ DECLARE n int; BEGIN\n  SELECT count(*) INTO n FROM pg_proc p JOIN pg_namespace ns ON ns.oid = p.pronamespace\n  WHERE ns.nspname = 'public' AND p.proname IN (\n    'methodological_assumptions', 'assumption_object_links', 'counterfactual_assessments',\n    'methodological_assumption', 'assumption_object_link', 'counterfactual_assessment'\n  );\n  IF n <> 0 THEN RAISE EXCEPTION 'SEC-N4 expected zero B4-owned functions, measured=%', n; END IF;\nEND $f$;\n"
  },
  {
   "id": "SEC-ACL-1-RLS-enabled-and-policies-exist-per-table",
   "sql": "DO $s$ DECLARE n int; BEGIN\n  SELECT count(*) INTO n FROM pg_class c JOIN pg_namespace ns ON ns.oid = c.relnamespace\n  WHERE ns.nspname = 'public' AND c.relname IN ('methodological_assumptions','assumption_object_links','counterfactual_assessments') AND c.relrowsecurity = true;\n  IF n <> 3 THEN RAISE EXCEPTION 'SEC-ACL-1 relrowsecurity measured=% expected=3', n; END IF;\n\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='methodological_assumptions';\n  IF n <> 3 THEN RAISE EXCEPTION 'SEC-ACL-1 methodological_assumptions policy count measured=% expected=3', n; END IF;\n\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='assumption_object_links';\n  IF n <> 2 THEN RAISE EXCEPTION 'SEC-ACL-1 assumption_object_links policy count measured=% expected=2', n; END IF;\n\n  SELECT count(*) INTO n FROM pg_policies WHERE schemaname='public' AND tablename='counterfactual_assessments';\n  IF n <> 3 THEN RAISE EXCEPTION 'SEC-ACL-1 counterfactual_assessments policy count measured=% expected=3', n; END IF;\nEND $s$;\n"
  },
  {
   "id": "MUT-PG-1-SELECT-policy-removal-caught-methodological_assumptions",
   "sql": "BEGIN;\nDROP POLICY methodological_assumptions_select ON public.methodological_assumptions;\nSET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a000000-0000-4000-8000-00000000000a\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.methodological_assumptions WHERE id='7a000000-0000-4000-8000-00000000007a')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'MUT-PG-1 methodological_assumptions measured=% expected=0 (SELECT policy removal must be caught)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-1-SELECT-policy-removal-caught-assumption_object_links",
   "sql": "BEGIN;\nDROP POLICY assumption_object_links_select ON public.assumption_object_links;\nSET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a000000-0000-4000-8000-00000000000a\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.assumption_object_links WHERE id='8a000000-0000-4000-8000-00000000008a')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'MUT-PG-1 assumption_object_links measured=% expected=0 (SELECT policy removal must be caught)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-1-SELECT-policy-removal-caught-counterfactual_assessments",
   "sql": "BEGIN;\nDROP POLICY counterfactual_assessments_select ON public.counterfactual_assessments;\nSET LOCAL ROLE uellix_app; SELECT set_config('request.jwt.claims', '{\"sub\":\"0a000000-0000-4000-8000-00000000000a\",\"role\":\"authenticated\"}', true);\nDO $a$ DECLARE v text; BEGIN\n  SELECT (SELECT count(*) FROM public.counterfactual_assessments WHERE id='9a000000-0000-4000-8000-00000000009a')::text INTO v;\n  IF v IS DISTINCT FROM '0' THEN RAISE EXCEPTION 'MUT-PG-1 counterfactual_assessments measured=% expected=0 (SELECT policy removal must be caught)', v; END IF;\nEND $a$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-baseline_availability",
   "sql": "BEGIN;\nALTER TABLE public.counterfactual_assessments DROP CONSTRAINT counterfactual_assessments_baseline_availability_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.counterfactual_assessments (organization_id, outcome_id, calculation_run_id, baseline_availability, basis_kind, deadweight_support_state, rationale, created_by) VALUES ('1a000000-0000-4000-8000-00000000001a','4a000000-0000-4000-8000-00000000004b','5a000000-0000-4000-8000-00000000005a','invented_value','documented_assumption','supported','mut2','0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 baseline_availability expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-basis_kind",
   "sql": "BEGIN;\nALTER TABLE public.counterfactual_assessments DROP CONSTRAINT counterfactual_assessments_basis_kind_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.counterfactual_assessments (organization_id, outcome_id, calculation_run_id, baseline_availability, basis_kind, deadweight_support_state, rationale, created_by) VALUES ('1a000000-0000-4000-8000-00000000001a','4a000000-0000-4000-8000-00000000004b','5a000000-0000-4000-8000-00000000005a','not_applicable','invented_value','supported','mut2','0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 basis_kind expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-deadweight_support_state",
   "sql": "BEGIN;\nALTER TABLE public.counterfactual_assessments DROP CONSTRAINT counterfactual_assessments_deadweight_support_state_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.counterfactual_assessments (organization_id, outcome_id, calculation_run_id, baseline_availability, basis_kind, deadweight_support_state, rationale, created_by) VALUES ('1a000000-0000-4000-8000-00000000001a','4a000000-0000-4000-8000-00000000004b','5a000000-0000-4000-8000-00000000005a','not_applicable','documented_assumption','invented_value','mut2','0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 deadweight_support_state expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-basis_type",
   "sql": "BEGIN;\nALTER TABLE public.methodological_assumptions DROP CONSTRAINT methodological_assumptions_basis_type_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.methodological_assumptions (organization_id, project_id, formulation, rationale, basis_type, materiality_flag, created_by) VALUES ('1a000000-0000-4000-8000-00000000001a','2a000000-0000-4000-8000-00000000002a','mut2','mut2','invented_value','material','0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 basis_type expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-2-CHECK-removal-caught-materiality_flag",
   "sql": "BEGIN;\nALTER TABLE public.methodological_assumptions DROP CONSTRAINT methodological_assumptions_materiality_flag_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.methodological_assumptions (organization_id, project_id, formulation, rationale, basis_type, materiality_flag, created_by) VALUES ('1a000000-0000-4000-8000-00000000001a','2a000000-0000-4000-8000-00000000002a','mut2','mut2','derived','invented_value','0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-2 materiality_flag expected the invalid INSERT to SUCCEED once the CHECK is dropped (n=%), proving the CHECK was load-bearing', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-3-conditional-NOT-NULL-removal-caught-baseline-available-fields",
   "sql": "BEGIN;\nALTER TABLE public.counterfactual_assessments DROP CONSTRAINT counterfactual_assessments_baseline_available_fields_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.counterfactual_assessments (organization_id, outcome_id, calculation_run_id, baseline_availability, basis_kind, deadweight_support_state, rationale, created_by)\n    VALUES ('1a000000-0000-4000-8000-00000000001a','4a000000-0000-4000-8000-00000000004b','5a000000-0000-4000-8000-00000000005a','available','baseline_observation','supported','mut3','0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-3 baseline_available_fields expected the incomplete-available INSERT to SUCCEED once the CHECK is dropped (n=%)', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-3-conditional-NOT-NULL-removal-caught-provenance-reference",
   "sql": "BEGIN;\nALTER TABLE public.methodological_assumptions DROP CONSTRAINT methodological_assumptions_provenance_reference_check;\nDO $m$ DECLARE n int; BEGIN\n  INSERT INTO public.methodological_assumptions (organization_id, project_id, formulation, rationale, basis_type, materiality_flag, created_by)\n    VALUES ('1a000000-0000-4000-8000-00000000001a','2a000000-0000-4000-8000-00000000002a','mut3','mut3','evidence_or_external_source','material','0a000000-0000-4000-8000-00000000000a');\n  GET DIAGNOSTICS n = ROW_COUNT;\n  IF n <> 1 THEN RAISE EXCEPTION 'MUT-PG-3 provenance_reference expected the missing-provenance INSERT to SUCCEED once the CHECK is dropped (n=%)', n; END IF;\nEND $m$;\nROLLBACK;\n"
  },
  {
   "id": "MUT-PG-4-EXECUTE-revocation-VACUOUS-BY-ABSENCE",
   "sql": "DO $f$ BEGIN\n  -- B4 introduces no SECURITY DEFINER function and no REVOKE EXECUTE\n  -- statement (see SEC-N4 above) — there is nothing to remove and no\n  -- non-vacuous mutation this control can perform. Recorded explicitly\n  -- as required, never as a silently-omitted control.\n  RAISE NOTICE 'MUT-PG-4: VACUOUS-BY-ABSENCE — B4 introduces no function/SECURITY DEFINER surface (see SEC-N4)';\nEND $f$;\n"
  }
 ]
}

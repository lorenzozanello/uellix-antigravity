// Twitter large-image card reuses the OpenGraph render so the summary_large_image
// card declared in app/layout.tsx metadata is never blank.
export { default, alt, size, contentType } from "./opengraph-image"
